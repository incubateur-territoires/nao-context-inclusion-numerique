# Changelog — dataspace

## 2026-09-22

- **`V172` [tech][migration]** — Schéma `llm` : ouvrir l'historique (fusions + journal MIN) au rôle read-only `nao_ro`. Avant : `nao_ro` n'avait `USAGE` que sur `llm`, `main`, `min`, `admin`, `reference` ; `audit` et `source` n'ont jamais été ouverts. L'outil LLM ne pouvait donc pas répondre aux questions d'historique — cas réel : la structure administrative 5048 (SIRET 24310069000037) « disparue » est en fait absorbée par la 1353 (SIRET 20006864100014) le 27/07/2026, information portée *uniquement* par `audit.structure_merge_log` ; aucune colonne métier ne relie les deux. Réponse produite : « structure introuvable ». Correctif : 3 vues curées `llm.structure_merge_log`, `llm.personne_merge_log`, `llm.evenement` (éclate `source.min__evenements.donnee` en `action` / `entity_id` / `user_id` / `valeur_avant` / `valeur_apres`), en `security_invoker = false` — aucun `GRANT` sur `audit` ni `source`, même patron que V102. PII : purge **récursive** (`llm.purger_pii`, objets et tableaux imbriqués — `personne_merge_log.moved_identifiers` est imbriqué) sur liste noire centralisée (`llm.cles_pii`), incluant le texte libre `presentation_detail` / `presentation_resume` des instantanés hérités du DAG `structures-similarities-merge` (forme « lieu » de la table LEGACY `main.structure`), où des adresses de contact rédigées à la main ont été relevées. Ajout de `COMMENT ON` sur les 3 nouvelles vues **et sur les 7 vues de V102/V103, qui n'en avaient aucune** : sans description, l'outil ne peut deviner ni que toute suppression est logique, ni que les id de `structure_administrative` et de `lieu_inclusion` se recouvrent (`1353` désigne les deux). Non-régression RGPD : `tests/test_llm_sans_pii.py` (aucune clé nominative, aucune clé JSONB non qualifiée, aucune adresse mail). **Relevé des 91 clés JSONB réellement présentes en base réelle** : 11 nominatives (`contact`, `nom`, `prenom`, `email`, `telephone`, `fonction`, `edited_by`, `deleted_by`, `decide_par`, `presentation_detail`, `presentation_resume`), toutes purgées ; `derniere_edition` et `editeur_utilisateur_id` qualifiées non nominatives ; et une clé **malformée `:suppression`** (2 occurrences, interpolation ratée côté MIN) purgée par précaution faute de savoir ce qu'elle transporte — à investiguer séparément. **Vérifié en production le 22/09** : le rôle Flyway (`sonum`) a bien `USAGE`+`SELECT` sur `audit` et `source` — les vues naissent opérationnelles. Il a aussi `SELECT` sur `min.*`, donc `llm.membre`, `llm.utilisateur` et `llm.structure` fonctionnent en prod ; le « permission denied » observé pendant l'analyse ne survient que sur une base **restaurée localement**, où le grant ne suit pas (correctif local : `GRANT SELECT ON min.membre, min.utilisateur, min.structure TO sonum;`). `U172` = rollback (drop des 3 vues et des 2 fonctions, commentaires remis à NULL). Refs #1591.

- **`V171` [carto][migration]** — **Correctif urgent de `V170`, mergée le même jour.** Le critère `personne_active` (`main.personne.deleted_at IS NULL`) est retiré de `expose`, et le motif `personne_supprimee` du `CASE`. Motif : `deleted_at` n'est pas un signal de fin d'activité. Il est écrit **par une source** — le flux Aidants Connect au retrait d'une habilitation, MIN par ailleurs — et n'est jamais effacé quand une autre source continue de faire vivre la personne. Mesure sur les 154 personnes que V170 retirait de `api.carto` : **154/154 avec un emploi actif hors `aidants-connect`**, 129/154 avec un contrat CoNum non rompu, 154/154 mises à jour par la coop *après* leur `deleted_at`, 144/154 avec `deleted_by = {aidants-connect}` seul. Aucune exception — c'étaient toutes des personnes en poste. Cas témoin, personne 1549 : `deleted_at` du 27/10/2022 par `aidants-connect`, affectations `coop` et `idposte` actives, compte coop vivant et visible, contrat CoNum 2023-09-26 → 2026-09-25 sans rupture ; V170 la retirait de la carte. Impact de V170 avant correction : 545 lieux perdaient un médiateur, dont **408 n'en affichaient plus aucun**. Périmètre rétabli : 2 059 → 2 213 personnes, 7 245 → 7 819 lignes, 6 078 → 6 486 lieux. La colonne `personne_active` subsiste (on ne retire pas une colonne par `CREATE OR REPLACE`), **informative uniquement**, son commentaire SQL le dit. **Non annulé** : l'adresse publiée reste `COALESCE(coop.users.email, mail_pro)`, sans `mail_perso` — cette moitié de V170 est saine. Docs corrigées dans la même MR, dont l'assertion fausse du §1.5 (« `deleted_at` écrit par MIN seulement ») qui est à l'origine de l'erreur. Spec carto : 25 cas, les deux cas `personne_supprimee` inversés, un `manque_personne_reellement_partie` ajouté (le besoin reste ouvert, le signal manque).

- **`V170` [carto][migration]** — `main.personne_visibilite_carto` : filtre de publication affiné, deux critères. (1) Nouveau critère `personne_active` (`main.personne.deleted_at IS NULL`) : jusqu'ici seul `compte_coop_actif` (`coop.users.deleted IS NULL`) était testé, or les deux notions sont distinctes. Colonne `personne_active` ajoutée en **fin** de vue (`CREATE OR REPLACE` n'autorise l'ajout qu'en queue, `api.carto` en dépend) et nouveau `motif_exclusion` `personne_supprimee`, évalué en premier dans le `CASE` — la répartition des autres motifs change donc mécaniquement (sur dev : `compte_coop_supprime` 334 → 1, `masque_coop` 916 → 869, `lieu_inactif` 707 → 626, sans que ces populations diminuent). (2) L'adresse publiée est construite sur les seuls champs professionnels, `COALESCE(coop.users.email, contact->'idposte'->>'mail_pro')` ; le repli sur `mail_perso` est retiré — garde défensive, ce repli n'était atteint par aucune des 7 245 lignes publiées (une personne publiée a par construction un compte Coop dont l'adresse est renseignée). Périmètre publié sur dev (réplique du 16/09) : **2 213 → 2 059 personnes**, 7 819 → 7 245 lignes (personne, lieu). Rapport de contrôle ajouté, lecture seule : `scripts/rapport_exposition_carto.sql`. Règle P-02 de `docs/cycle-de-vie.gardes.yml` modifiée. `api.carto` non redéfinie (elle agrège la vue), pas de `NOTIFY pgrst`.

- **`V169` [carto][migration]** — Le carto-dag est à l'arrêt depuis le 2026-09-16 : `NotNullViolation` sur `staging.carto__structures.pivot` au chargement du silver. Cause : la cartographie nationale a cessé de publier le champ `pivot` (SIRET/RNA du lieu). Fichier du 2026-09-22 : **16 010 lieux sans pivot sur 18 756 (85 %)**, contre 18 812/18 812 au run du 2026-09-15 — comparaison par id entre les deux millésimes : 15 637 ids communs ont PERDU leur pivot, volume total inchangé (ce n'est pas une troncature, la garde volumétrique #1724 ne se déclenche pas). **V169** : `pivot` passe NULLABLE dans le silver, plutôt que de rejeter les lignes (85 % du fichier écarté → garde volumétrique → dag toujours à l'arrêt, pour un champ sans consommateur). `pivot` n'a plus d'usage métier depuis #1711 : `api.carto` et `api.get_carto_mediateur` l'exposent en constante `'00000000000000'` (V123/V147/V154/V165) ; seul lecteur de la colonne, `scripts/rapport_carto_integration.py` (doublons par (pivot, nom)), déjà NULL-safe — il détectera mécaniquement moins de doublons tant que l'amont ne republie pas le champ. Contrat `contracts/carto__structures.yml` mis à jour (`pivot` → `required: false`, observation datée). Rejeu à blanc du chargement complet sur dev (transaction + ROLLBACK, fichier réel du 22/09) : 18 734 lignes insérées (22 rejets `code_insee_absent`, inchangé), 2 745 pivots restants, aucune autre contrainte violée. ⚠️ À signaler à l'amont : la perte du SIRET/RNA sur 85 % des lieux est une régression de qualité du fichier national, même si le dataspace n'en dépend plus.

- **`!1000` [aidants-connect]** — Le DAG `aidants-connect-import` échouait depuis le 17/09 (`UniqueViolation` sur `structure_administrative_siret_antenne_ukey`, clé `(null, CHATEAU RENARD)`) : plus aucune mise à jour des aidants, des structures ni des affectations depuis cette date. Cause : `main.structure_administrative` porte deux contraintes uniques que cette ingestion peut violer — `structure_ac_id_ukey` et `siret_antenne_ukey` (**NULLS NOT DISTINCT**) — et un `ON CONFLICT` n'en cible qu'une ; le batch des structures **sans SIRET** conflitait sur `structure_ac_id`, donc rien ne couvrait `(NULL, denomination_antenne)`. Déclencheur source : AC a renommé la structure `f67f296e…` en « France services La Poste de Château-Renard » puis créé une nouvelle structure `70d4706c…` au nom libéré « CHATEAU RENARD », nom que la SA `11145` porte toujours (#1743 : le nom appartient à la structure une fois créée, pas à la source) ; un aidant du delta la référençant a fait passer le gate lazy et l'`INSERT` a emporté tout le run. Même bug qu'au 31/07 avec `(null, HABITAT JEUNES CANTAL)`, jusqu'ici documenté en commentaire comme « bug latent non corrigé ». Après : `ingest_structures` déduplique en Python sur **toutes** les clés uniques avant le batch `INSERT` (règle `CLAUDE.md`) — `structure_ac_id` intra-lot, `(siret, antenne)` intra-lot, `(NULL, antenne)` déjà en base (lookup **sans** filtre `deleted_at` : une SA soft-deletée occupe la clé), `structure_ac_id` déjà porté par une autre SA — et écarte les collisions vers `staging.rejets` (flux `ac__structures`, étape `ingest` ; motifs `antenne_sans_siret_deja_en_base`, `antenne_sans_siret_doublon_intra_batch`, `structure_ac_id_doublon_intra_batch`, `structure_ac_id_deja_sur_autre_sa`) avec l'id de la SA occupante, au lieu de faire échouer le run ; le shell écrit les rejets après `conn.commit()` (le sink commite lui-même). La structure écartée n'est pas créée : son affectation est comptée dans les `skipped` d'`ingest_personne_affectations`, l'arbitrage (fusion / renommage dans MIN) est métier et n'est pas deviné sur l'homonymie. Reproduit sur la copie dev du silver du 16/09 (transaction rollbackée) : avant = `UniqueViolation` à `run_sa_batch`, après = `inserted=0, ecartes=1` + rejet pointant la SA `11145`. **Ampleur mesurée** sur la copie dev du silver du 16/09 : 65 structures AC sans SIRET absentes de SA, dont **16 en collision** de nom avec une SA existante sans SIRET, sur **7 noms distincts** (dont 11 homonymes de « FRANCE SERVICES  MAUZÉ SUR LE MIGNON », doublons source, et « HABITAT JEUNES CANTAL » — celui du 31/07, toujours là) : le run serait retombé sur la suivante dès CHATEAU RENARD réglé. Tests d'intégration `TestIngestStructures` remis en état au passage — ils appelaient encore `ingest_structures(conn, csv_path)`, signature abandonnée au lot 2 caches, et échouaient tous les 11 ; 3 cas de régression ajoutés. Assertion **T-02** du cycle de vie inchangée (robustesse, pas d'évolution de règle).

## 2026-09-21

- **`(en cours)` [doc][coop]** — Règle de publication coop → fichier national ajoutée aux docs cycle de vie (technique §1.6/§2.4, support §1/§4) : un lieu coop n'est transmis à mednum-cli que s'il est visible, avec un médiateur visible en activité et au moins un service (`lieux-publies.query.ts`). Mesure du 21/09 sur les 943 « purs coop visibles » non référencés : ~740 sans médiateur en activité, 184 sans adresse exploitable, 7 rejetés à l'ingestion (`code_insee_absent`), 5 avec compte coop supprimé, 8 à instruire. Refs SEPT #1943.

## 2026-09-18

- **`18e54af` [carto][api-carto][coop][migration]** — L'état d'un lieu (visible, supprimé) appartient aux outils de gestion, pas au fichier national. Le SQL de la tâche `integration_lieux` du carto-dag est extrait dans `etl/load/carto_integration_lieux.py` et exécuté tel quel par une spécification sur base (`tests/carto/cas_cycle_de_vie_lieux.yml`, 14 cas). Avant : la réactivation nocturne remettait `visible = true` sur toute ligne non coop présente au fichier (y compris supprimée), et le déréférencement éteignait toute ligne non coop visible sans `carto_id` → un lieu masqué ou supprimé dans MIN revenait la nuit suivante, un lieu créé dans MIN serait éteint. Après : cycle de vie restreint aux lignes externes (`structure_coop_id IS NULL AND updated_at_min IS NULL`), jamais de rallumage d'un lieu supprimé, `visible` retiré du SET des données (la branche données ne porte plus d'état). **V167** : `api.carto` filtre `deleted_at IS NULL` pour toutes les origines (avant : lignes coop seulement) — 18 774 lignes servies avant comme après (0 ligne mednum supprimée-visible-référencée au 16/09). Filet `registre_lieux_coop.py` : balayage des orphelins (`SQL_RETIRER_ORPHELINS` : inscription vivante dont le lieu coop a disparu physiquement → `deleted_at`, identité conservée, compteur `orphelins_retires`, warning) — 5 orphelins en prod issus du job coop `update-structures-cartographie-nationale` (run du 11/09, retiré depuis) ; contrôle de santé §7. Spécification du filet entier sur base : `tests/coop/test_filet_registre_lieux.py`. Docs : doctrine « état ≠ données » (§1.3), MIN écrivain des lieux mednum et lieux coop en lecture seule dans MIN, piège n° 8 réécrit, piège n° 10 (fraîcheur globale), L-05/L-06/L-10 et A-02 de la matrice, manifeste. Refs SEPT #1950 (prérequis de #1951 et #1495).

## 2026-09-17

- **`c6df1ef` [doc]** — Règles du cycle de vie lieux & personnes : trio de documents versionné (`docs/cycle-de-vie-lieux-personnes.md` technique, `-support.md` sans jargon, `-preuves.md` matrice assertion → modules → preuve avec statuts honnêtes ✅ TU-CI / 🧪 TI-manuel / 🔎 sondé / 📄 constaté / 🤝 chez la coop / ❌ à créer), manifeste des porteurs `docs/cycle-de-vie.gardes.yml`, job CI `docs-guard` en warning (`scripts/docs_guard.py` : porteur touché sans mise à jour des docs → échec du job) et skill agent `regles-lieux-personnes`. Volumétrie mesurée 17/09 : 24 307 lieux vivants (11 539 purs mednum / 7 761 mixtes / 5 007 purs coop), 18 769 sur api.carto, 2 342 médiateurs exposés sur 8 308 lieux. Refs SEPT #1948 (issu de #1724).

## 2026-09-16

- **`(en cours)` [coop][migration]** — SEPT #1724 : listes vides — `{}` et NULL équivalents dans les comparaisons référentiel ↔ coop (V166 + filet). Constat du contrôle de santé prod du 15/09 : la double écriture coop pose `[]` pour une nomenclature vide (listes scalaires Prisma non-nullables — elle ne PEUT pas écrire NULL) là où l'historique du référentiel porte NULL (`_normalize_array` carto, backfill V160) → ~50 fausses divergences (un lieu aux nomenclatures vides divergeait d'un coup sur 8 champs) et un ping-pong quotidien filet ↔ double écriture qui aurait empêché `metier_rafraichi` de retomber à 0 (notre signal d'alarme). Convention actée : « vide » = `{}` OU NULL, les deux formes cohabitent, les COMPARATEURS normalisent les deux côtés — `main.lieu_divergences_coop` recréée (V166) et `WHERE` du rattrapage métier du filet (11 colonnes listes en `NULLIF(..., '{}')`). Aucune action côté coop (informée). Testé sur dev : `{}` façon coop → 0 divergence vue + 0 candidat filet ; vraie différence → toujours détectée des deux côtés.

## 2026-09-15

- **`V165` [api-carto][tech][migration]** — SEPT #1940 : la règle « qui est visible sur la carte » est formalisée dans une vue dédiée `main.personne_visibilite_carto` (V165), une ligne par (personne, lieu) de `main.personne_affectations_lieu`, avec chaque critère en colonne (`lieu_actif`, `compte_coop_actif`, `visible_coop`, `est_cn`, `est_ac`, `a_emploi_actif_non_ac`, `emploi_autorise`), un booléen `expose` et un `motif_exclusion` (lieu_inactif, compte_coop_supprime, masque_coop, cn_sans_emploi_actif). `api.carto` agrège désormais cette vue (`WHERE expose`) au lieu de recalculer les flags dans son CTE. **Iso-comportement V164** : sortie `api.carto` identique avant/après sur dev (18 769 lieux, 7 810 entrées médiateurs, 0 écart). Répartition dev : 9 781 lignes exposées ; 1 954 masquées par le médiateur, 1 601 lieux inactifs, 579 comptes coop supprimés, 475 CN sans emploi actif. Spécification exécutable `tests/carto/cas_visibilite.yml` (20 cas, statut `regle` | `manque`, motif attendu) + `tests/carto/test_visibilite_api_carto.py` (transaction + ROLLBACK, `DATABASE_URL`) ; les manques connus (CN à contrats rompus encore employé pour la coop, structure AC supprimée) sont en `xfail` strict. Bloc défensif sans schéma coop. Note de vulgarisation `visibilite_api_carto_20260914.md`.

## 2026-09-11

- **`(en cours)` [api-carto][coop][migration]** — SEPT #1868 : `api.carto.mediateurs` lit les coordonnées, la visibilité et l'existence des médiateurs **en direct dans la coop** (`coop.users.email/phone/deleted`, `coop.mediateurs.is_visible`) au lieu de la copie `main.personne` (V164). Cause : depuis le décommissionnement de la branche personnes du coop-dag (`f372fe3`, 11/08) et la doctrine ADR-002 coop (« pas de synchro continue de `main.personne` »), plus rien n'écrit `contact->'coop'` ni `is_visible` : copies gelées au 11/08 — une médiatrice ayant retiré son téléphone de ProConnect restait affichée avec l'ancien numéro sur la cartographie nationale. Mesure dev (3 766 personnes coop) : 4 téléphones retirés côté coop encore servis, 107 ajoutés jamais servis, 11 changés, 16 emails changés, 118 drapeaux de visibilité divergents, 51 médiateurs inscrits après le 11/08 absents de la carte (`is_mediateur` jamais posé), 2 comptes supprimés encore servis. Nouvelle fonction `main.normaliser_telephone(text)` = port SQL strict de `format_and_validate_phone` (0 écart sur 2 378 téléphones coop distincts). `CREATE OR REPLACE` (OID conservé), bloc défensif sans schéma coop. Effet sur api.carto dev : 682 lieux / 18 769 modifiés — 3 téléphones retirés, 20 ajoutés, 82 changés (paires médiateur×lieu), 20 emails, 26 médiateurs ajoutés, 10 retirés ; 0 téléphone obsolète, 0 masqué/supprimé restant ; vue ~2× plus rapide (0,5 s vs 1,1 s). Reste, même cause : `dataviz.personne`/`dataviz.poste` lisent encore `contact->'coop'` ; nom/prénom toujours issus de `main.personne`.

## 2026-09-10

- **`(en cours)` [coop][migration][tech]** — SEPT #1724 : **renommage `main.lieu_inclusion_registre` → `main.lieu_inclusion`** (V163, accord coop du 10/09 — leur PR #615, non déployée, vise le nouveau nom). La projection triviale V162 et son trigger `INSTEAD OF` disparaissent : la TABLE reprend le nom historique et devient l'objet unique lu et écrit par tous (les éditions Prisma MIN frappent la table — leur modèle déclare `updated_at` en `dbgenerated`, jamais écrit, vérifié). Cérémonie pattern V153, générée depuis le catalogue : 14 vues consommatrices droppées/recréées à l'identique (elles se rebindent sur la table), commentaires et grants restaurés (postgrest_*, app_metabase, app_python) ; les vues liées par OID (`personne_affectations_lieu`, `activites_coop`, `lieu_divergences_coop`) suivent automatiquement ; contraintes déjà aux noms historiques, séquence d'identité renommée ; `sed` de 5 fichiers Python (dags, filet, load). Seule évolution de comportement : **api.carto exclut les lignes coop supprimées** (`deleted_at` — le filtre que portait la projection, re-logé ; 0 ligne concernée à ce jour, nécessaire dès le déploiement coop). Validé sur dev : contenu 0 diff sur 23 428 lignes, api.carto 0 diff sur 18 394, opendata iso, édition directe OK, filtre suppression OK. ⚠️ Séquencement : merger après !984/!985 ; la coop ne déploie #615 qu'après confirmation du renommage en prod.

## 2026-09-09

- **`(en cours)` [coop][carto][api-carto][migration]** — SEPT #1724 : **BASCULE — le référentiel des lieux est servi depuis la table** (V162). `main.lieu_inclusion` (vue d'union V153, 2 branches + jointure coop live + compteur calculé) est remplacée EN PLACE (`CREATE OR REPLACE`, mêmes 38 colonnes) par une **projection triviale** de `main.lieu_inclusion_registre` — les 15 vues dépendantes, Prisma MIN et le trigger `INSTEAD OF` survivent tels quels ; la table garde son nom (le `@@map` de la PR coop #615 pointe dessus, renommage = nettoyage ultérieur coordonné). Alignements : `visible_pour_cartographie_nationale` des lignes coop := **drapeau coop** (1 018 lieux voulus visibles récupèrent leur visibilité — aucun n'a de carto_id → api.carto INCHANGÉE en cardinalité, +1 018 dans l'export opendata = photo complète, cible cas 4 ; les 20 flips inverses étaient déjà masqués par le AND) ; `mediateurs_en_activite` := compte réel, rafraîchi quotidiennement par le filet (`SQL_RAFRAICHIR_COMPTEURS`, J+1 assumé). **Filet complété d'un rattrapage MÉTIER par comparaison de valeurs** (`SQL_RAFRAICHIR_METIER`) — leçon du 2026-09-11 en prod : un lieu coop créé reçoit une identité sans métier avec `updated_at_coop = modification`, invisible à vie d'une garde par date (cas « Restos du cœur Lure », + 13 divergences de fenêtre en une matinée). Le rattrapage compare les 20 champs (mêmes expressions que le backfill, contact combiné sans retrait de clé, visible = drapeau coop, deleted_at miroir) : ceinture de la fenêtre jusqu'au déploiement coop #615, puis alerte (>0 ligne = un chemin d'écriture contourne la double écriture). Testé : remplissage d'une identité nue, rattrapage d'édition fraîche, idempotence. **V154 abrogée** : api.carto/opendata reviennent à la lecture pure du référentiel — fin de la divergence api↔MIN. Diff api.carto mesuré (18 394 lignes, 0 perdue/gagnée, 7 674 fiches coop changent de contenu) : date_maj 7 652 (sémantique référentiel), labels zonage 2 696 (abandon acté 19/08 atteint l'export), contact 127, services 146, typologies 77, horaires 57, **nom 16** (le silver portait déjà la graphie coop), **source 0**. Non-régression vue : 23 428 lignes iso, **changements = 1 018 visibilité + 0 compteur + 0 autre champ** (le matérialisé ≡ le calculé). Éditions MIN sur lieux coop désormais effectives (limitation V153 refermée). Perf : liste triée 58 ms → **0,97 ms** (le ×33 de l'union disparaît). Effet bonus refermé aussi : les 1 829 réparés + contacts combinés servis partout.
- **`(en cours)` [carto]** — SEPT #1724 cas 4 : le cycle de vie carto n'écrit plus les lignes coop. Le déréférencement nocturne (`visible=FALSE`, `carto_id=NULL` pour les absents du fichier national) et la réactivation forcée (`visible=TRUE` pour les présents) sont restreints à `structure_coop_id IS NULL`. Motif : la double écriture coop (PR #615) pose l'interrupteur de partage du médiateur dans `visible_pour_cartographie_nationale` — sans restriction, le run nocturne ferait du ping-pong avec lui (re-masquage d'un lieu publié absent du fichier, ré-exposition d'un lieu masqué présent). Règle actée : la vie d'un lieu coop ne dépend jamais de sa présence au fichier national — visibilité = son drapeau, carto_id conservé (continuité si le record revient). Le lien d'identifiant (rattachement par segment coop) reste actif. NB : l'alignement du stock (les 524 voulus-visibles historiquement déréférencés) se fera à la bascule de la vue, pas ici.
- **`(en cours)` [coop][migration]** — SEPT #1724 : vue `main.lieu_divergences_coop` (V161), support du garde-fou d'édition. Décision : la divergence référentiel ↔ coop n'est jamais arbitrée en silence — l'utilisateur qui modifie un lieu coop (app coop, MIN) doit d'abord synchroniser. La vue sert une ligne par (lieu coop vivant, champ divergent), côté coop normalisé comme V160 l'écrit (une ligne = une vraie divergence de contenu, pas un artefact de format), avec valeurs des deux côtés, signature et dates. Grants SELECT conditionnels aux rôles `coop` et `min_scalingo`. Double usage : garde-fou applicatif + contrôle de fidélité de la matérialisation V160 — mesuré sur dev : **0 divergence sur 12 708 lieux × 20 champs** (le miroir est exact), détection d'une divergence injectée vérifiée. Divergences attendues en prod : les clés de contact comblées depuis l'archive mednum (~330 lieux). Contrat coop v2 rédigé (`contrat_coop_lieux_v2_20260909.md`) : la coop a depuis implémenté sa double écriture (PR #615) qui va au-delà du contrat.

## 2026-09-08

- **`(en cours)` [coop][migration]** — SEPT #1724 : la coop écrit le registre — grants + re-matérialisation (V160). Décision 2026-09-04 (« même donnée partout, le dernier qui parle a raison »), modèle affiné 2026-09-09 : la coop garde sa vérité, le référentiel (`main.lieu_inclusion_registre`) porte la vérité pour tous les autres, la coop lui « dit » ses écritures. Le « dit » est implémenté PAR LA COOP (PR coop-mediation-numerique #615, draft) : double écriture applicative en transaction Prisma — update par lien coop / ADOPTION d'une inscription sans lien (anti-doublon) / insert, colonnes par chemin, adresse via NOTRE `trouver_ou_creer_adresse_lieu` (V155), `source='Coop numérique'` à chaque écriture, revendication prudente du carto_id, `deleted_at`. Un trigger dataspace (v1 de la MR) a été développé puis RETIRÉ : miroir intégral, il aurait écrasé leur discipline par chemin et cassé l'adoption sur la contrainte unique. Rôle de V160 : (1) **grants du rôle `coop`** — ⚠️ correction post-échange Marc (2026-09-10) : l'app coop se connecte en `sonum`, PROPRIÉTAIRE de la table (aucun grant ne la contraint — sa protection est son typage `ColonnesDuRegistre`, vérifié sur le SQL émis) ; les grants posés restent le périmètre d'écriture OFFICIEL des colonnes coop, prêts pour une future connexion sous rôle borné (gouvernance à instruire) + EXECUTE sur la fonction d'adresse ; (2) **re-matérialisation du stock** (~12 700 lignes jamais touchées par leur code, PR non déployée) avec **combinaison du contact clé par clé** — la coop gagne, l'archive (fusions mednum d'avant l'étape 4, éteinte des fusions par `mergeOldLieux` : ~330 lieux, 261 téléphones, 298 courriels, 46 sites web) comble les vides ; rien n'est écrit chez la coop, leur difftool proposera ces valeurs à l'édition (garde-fou : l'humain tranche) ; (3) nettoyage du trigger v1 (dev). Testé sur dev : écritures métier sous `SET ROLE coop` OK, `updated_at_carto` refusé, fonction adresse OK. Séquencement corrigé (2026-09-10) : la coop déploie indépendamment (sonum propriétaire) ; seule contrainte = la restriction du cycle de vie carto avant/peu après leur déploiement (ping-pong visibilité, borné tant que la vue ANDe).

## 2026-08-28

- **`(en cours)` [coop][migration][tech]** — SEPT #1724 lot 2 / #1707 : **fin de l'import des structures coop par API** (V159). Le coop-dag ne maintient plus que l'identité des lieux dans `main.lieu_inclusion_registre` via un filet SQL direct sur `coop.lieu_inclusion` (`etl/load/registre_lieux_coop.py`, tâche `reconcilier_registre_lieux`) : INSERT des lieux vivants sans ligne registre, résolution d'adresse par `main.trouver_ou_creer_adresse_lieu` (V155), rafraîchissement sur `modification` (trois requêtes séquentielles, idempotent, no-op sans schéma coop). Même fonction et mêmes colonnes que le lot 1 du contrat d'écriture synchrone — le filet fait le travail à J+1 tant que la coop ne l'a pas codé, puis sert d'alerte. Mesuré sur dev (copie du 11/08) : 4 lieux insérés, 399 adresses complétées, 159 rafraîchis, 12 non résolvables (sans code INSEE/CP/commune), 0 écart résiduel, 12 712 lieux coop servis par la vue (12 708 avant). Retirés : `fetch_all_structures`, `transform_structures`, `geocode_new_lieux` (BAN), `ingest_structures`, `build_role_index`, param `freeze`, `working_dir`, `transformer_structures`/`consolider_structures_coop` du core et leurs tests, contrat `coop__structures`. V159 droppe `source.coop__structures` (342 MB), `source.coop__activites` (orpheline depuis V144, 566 MB) et `staging.coop__structures`, purge les rejets `coop__structures`. Exception médaillon documentée (`approche-data/01`, `19`) : pas de bronze pour une source hébergée dans le même cluster. Le fetch utilisateurs subsiste sans consommateur (MR suivante).
- **`(en cours)` [coop][migration]** — SEPT #1707 : `main.lieu_inclusion.emplois` passe à `NULL` pour les lignes coop (V158). V155 comptait ce champ sur `coop.employes_structures` (uuid partagé lieu/employeuse), table **abandonnée** par la coop depuis la bascule « pur main » (plus aucune écriture depuis le 06/08, DROP prévu à l'échange final) : compteur figé aujourd'hui, vue cassée demain. Recompter depuis `main` via `structure_coop_id` ne couvrirait que le stock (3 127 lieux / 12 712) et ressusciterait le lien lieu↔SA écarté en #1711 ; un nombre d'emplois est une notion d'employeuse, pas de lieu → `NULL` comme `dataviz.structures`. Aucun consommateur de la colonne (pg_views, code MIN, DAGs). `mediateurs_en_activite` reste calculé sur `coop.mediateurs_en_activite` ; lignes non-coop inchangées (registre).
- **`(en cours)` [coop][migration][api]** — SEPT #1707 (phase 5) : suppression de `main.coordination_mediation` et de l'endpoint `api.get_mediateur(email)` (V157). La table était la réplique (coop-dag, `insert_coordination_mediation`) de `coop.mediateurs_coordonnes`, désormais dans le même cluster ; elle divergeait déjà (3 655 liens actifs côté main vs 3 391 côté coop au 11/08 — suppressions jamais propagées, même symptôme que V144/V151). Son unique consommateur, la RPC `get_mediateur` (rôle `postgrest_coop`, clé `conseillers_numerique_coordonnes`), n'avait qu'un client : l'application coop, qui l'a retiré le 2026-08-20 (commit coop `924ff99e`) — elle lit `main` directement. Plutôt qu'une vue de compatibilité pour un endpoint orphelin : drop des deux. Rôle `postgrest_coop` conservé (`auth`). Côté ETL : tâche `insert_coordination_mediation` retirée du coop-dag, `transformer_utilisateurs` ne produit plus la clé, colonne `staging.coop__utilisateurs.coordination_mediation` droppée, contrat `coop__utilisateurs` mis à jour. Le fetch `/api/v1/utilisateurs` ne sert plus qu'à `build_role_index`. À suivre côté MIN : retirer le modèle Prisma `coordination_mediation` (et ses deux relations sur `personne`).

## 2026-08-26

- **`(en cours)` [migration]** — SEPT #1845 : grants `min_scalingo` pour l'interface de revue des appariements de lieux (PR MIN #1853). V156 : `SELECT` sur `main.lieu_appariement` + `UPDATE` **limité aux colonnes de décision** (`statut`, `decide_par`, `decide_le`) — le gel du reste de la table (scores, segments, détections, alimentés par le DAG `lieu-appariement`) est appliqué par PostgreSQL, pattern V155. Ni INSERT ni DELETE (table alimentée/purgée exclusivement par le DAG ; les statuts `valide`/`rejete` sont préservés à chaque run). Grant conditionnel à l'existence du rôle (CI `test_migration` sur instance fraîche). Vérifié sur dev sous `SET LOCAL ROLE min_scalingo` : UPDATE des 3 colonnes OK, UPDATE d'un score et DELETE refusés (`permission denied`). ⚠️ À merger au moment du déploiement de la PR MIN #1853, pas avant (merge sur `main` = prod auto).

## 2026-08-24

- **`(en cours)` [carto]** — Fix crash prod du carto-dag (run du 2026-08-24 02:00) : `UniqueViolation lieu_inclusion_structure_coop_id_ukey` dans `integration_lieux`. Le bloc « 0b double match » (un record du fichier national matche par carto_id une ligne A du registre ET par coop_id une autre ligne B) posait le `structure_coop_id` sur A **avant** de le retirer de B — violation garantie de la contrainte UNIQUE (non déférable) dès que le cas se présente. Bug latent depuis `ff3d1c6` (réécriture séquentielle), jamais déclenché : les lignes B (coop sans carto_id) n'existaient pas… jusqu'à ce que le filet `reconcilier_identites_coop` (V155) crée ~1 800 identités coop le 20/08 — dont les « 128 trace carto perdue » de l'analyse des 494, toujours présents au fichier national sous un id fusionné (ex. le cas planté : `Coop-numérique_02a7c4df…__France-Services_3396`, Louviers) rattaché à une vieille ligne carto. Correctif aligné sur l'architecture #1724 : le transfert est **inversé** — la ligne coop B (porteuse de la vérité, de la vue et des activités) reçoit le carto_id ; la ligne carto A le perd et est désactivée par la désactivation des absents en fin de transaction (doublon dormant, candidat GC/similarités). Ordre retirer-puis-poser imposé par les contraintes UNIQUE. Prouvé sur dev : reproduction synthétique de la violation, puis rejeu de la transaction complète sur données réelles — 128 transferts, 0 double match restant, 0 insert parasite.

- **`(en cours)` [coop][migration]** — SEPT #1724 : lot 1 de l'écriture synchrone coop (contrat accepté). V155 : fonction `main.trouver_ou_creer_adresse_lieu(...)` (SECURITY DEFINER, lookup prioritaire par clef d'interopérabilité BAN — le `ban_id` coop est une clef interop, pas un uuid —, repli clef naturelle alignée sur `integration_adresses`, création idempotente, testée sur dev) ; droits du rôle `coop` sur le registre **limités aux colonnes d'identité** par grants de colonnes (structure_coop_id, adresse_id, nom initial NOT NULL, edited_by, updated_at_coop — le gel des colonnes métier est appliqué par PostgreSQL) ; branche coop de la vue : filtre `cl.suppression` (la fiche d'un lieu supprimé côté coop disparaît, la ligne registre reste) + compteurs **calculés** depuis `coop.mediateurs_en_activite`/`employes_structures` (plus de dépendance aux compteurs dénormalisés qui gèleront avec le coop-dag). Nouveau filet quotidien `reconcilier_identites_coop` dans le DAG `lieu-appariement` (répare + alerte tout lieu coop sans ligne d'identité ; assure aussi la transition tant que l'app coop n'écrit pas). **Premier run dev : 1 829 lieux coop actifs SANS AUCUNE identité entrepôt réparés** (invisibles de MIN et de tout l'écosystème — dont 497 voulus visibles et 144 avec médiateurs actifs ; cause vérifiée (bronze/silver/transformer, API coop hors de cause) : délégation au flux carto dans `transformer_structures` (494 lieux visibles/carto-liés exclus du silver, jamais arrivés par le fichier national) + sélection par rôle utilisateur pour les 1 335 autres (aucun déclarant au payload)), 4 restants sans adresse exploitable (signalés, à remonter coop). `api.carto` inchangée (pas de carto_id). Légitimité vérifiée : 0 lieu inerte — 91 % portent des activités, 100 % modifiés en 2026. **Effet bonus : 513 979 activités (794 647 accompagnements, ~13 % du total) avaient `lieu_id` NULL et gagnent leur rattachement** — rapports par lieu/département corrigés d'autant.

## 2026-08-19

- **`(en cours)` [carto][api-carto]** — SEPT #1724 : l'export public repasse en **photo mednum prioritaire sur les fiches coop** (V154, posture TRANSITOIRE). `api.carto`, `opendata.lieux_mednum` et `opendata.lieux_geojson` servent les champs métier depuis le silver `staging.carto__structures` quand le record est au fichier national du run courant ET que la ligne est un lieu coop — les lieux externes restent servis depuis le registre (déjà la photo mednum arbitrée, éditions MIN préservées : 0 écart mesuré vs l'avant-chantier sur les 10 720 lignes externes) (normalisation identique à l'ancien `_CARTO_COMMON_SET`), repli sur `main.lieu_inclusion` sinon. Motif : la bascule V153 avait changé la nature des fiches coop de l'export public (vérité coop) sans coordination avec les consommateurs externes ; tant que la décision métier du canal n'est pas prise, l'export ne change pas de nature — MIN/dataviz continuent de lire la vérité coop (divergence assumée, temporaire, réversible U154). Validé vs la baseline d'avant-V153 : contenu identique aux résidus voulus près (-20 lignes du fix visibilité ; `date_maj` = date déclarée par la source au lieu de l'heure du pipeline — plus conforme data-inclusion). ⚠️ Résidu documenté : les 20 lieux masqués restent dans `opendata.lieux_mednum` (filtre V145 `carto_id OU visible`) et dans le fichier national amont — alignement du filtre opendata à trancher.

- **`(en cours)` [coop][carto][migration]** — SEPT #1724 étape 5.2 : **`main.lieu_inclusion` devient la VUE D'UNION** (V153). La table est renommée `main.lieu_inclusion_registre` (ids publics, clefs, adresse_id, timestamps, cycle de vie — colonnes métier conservées jusqu'à l'étape 5.4 comme archive). La vue sert les lignes coop **en direct depuis `coop.lieu_inclusion`** (vérité coop) et les autres lignes depuis le registre. Décisions actées : nom **brut coop** (283 graphies changent), labels de zonage FRR/QPV **abandonnés** sur les lieux coop (2 786 lieux — l'info reste dans `admin.zonage`/est_qpv/est_frr ; **seuls les consommateurs d'api.carto/data.gouv perdent ces marqueurs sur les lieux coop**), contacts issus des fusions mednum **perdus** (478 — archivés dans le registre, premier lot du canal étape 6), convention `contact = {}`. Gains immédiats : **+1 672 lieux « Conseillers numériques »** que la réplique avait perdus (compteurs labellisés 10 515→12 180, conseillers 7 736→9 408), fraîcheur affichée alignée sur la source (`updated_at_coop = GREATEST(réplique, cl.modification)`). Mécanique : 17 vues dépendantes recréées (OID) dont `personne_affectations_lieu` et `activites_coop` rebranchées sur le registre ; écritures dags re-ciblées registre (ON CONFLICT impossible via une vue) ; UPDATE Prisma MIN via trigger `INSTEAD OF` (testé, RETURNING ok ; 0 édition MIN sur lieu coop depuis juillet) ; upsert lieux du coop-dag réduit à l'identité (adresse_id, compteurs, updated_at_coop — les valeurs métier ne sont plus répliquées, le registre est gelé en archive). Bloc défensif `to_regclass` (base neuve CI). Non-régression : baseline 382 lignes, 6 écarts, tous = les décisions ci-dessus. **Visibilité** : la vue sert `visible = cycle de vie carto ET drapeau coop` → corrige 20 lieux exposés sur api.carto contre le choix du médiateur (18 414 → 18 394) ; 524 lieux voulus visibles côté coop restent masqués (absence structurelle du fichier national — analyse et pistes dans `analyse_bascule_vue_union_v153.md`).

- **`(en cours)` [carto][coop]** — SEPT #1724 étape 4 : le flux carto n'écrit plus JAMAIS les données métier des lieux coop. L'UPDATE métier d'`integration_lieux` exclut désormais `structure_coop_id IS NOT NULL` — seuls le lien d'id (rattachement carto_id), la réactivation et le déréférencement restent actifs pour eux (cycle de vie carto préservé, la coop reste source mednum → carte nationale intacte). Contexte : le coop-dag ingère déjà tous les lieux coop en direct avec sa garde de fraîcheur symétrique (constat « délégation » périmé) ; ne subsistaient que les écrasements par écho/fusion externe passant la garde (~30 cas mesurés au 2026-08-17) et la pollution d'union des fusions mednum (résurrection de typologies supprimées côté coop). Boucle de rétroaction morte structurellement ; les MAJ externes sur lieux coop seront routées par le canal d'appariement (`main.lieu_appariement` V152, décision métier en cours sur requête-de-modification vs écriture arbitrée). Effet différé : contra les prochains runs carto (0 écriture bloquée sur l'état dev actuel, déjà intégré).

- **`(en cours)` [carto][coop][migration]** — SEPT #1724 étape 3 : matching maison lieux coop ↔ records mednum. Nouvelle table `main.lieu_appariement` (V152) = mémoire durable du rapprochement (le silver carto est TRUNCATE par run et les ids composites mednum churment à chaque fusion), clé = **segment d'id** (id par source, stable) × lieu. Nouveau DAG `lieu-appariement` (quotidien 12:00, pattern personne-reconciliation) : (1) preuve par segments — chaque segment d'un record fusionné avec un id coop est rattaché au lieu coop, statut `auto` (9 647 segments / 7 670 records au premier run dev) — cette mémoire survivra au retrait de la coop des sources mednum ; (2) candidats par similarité entre records purement externes et lieux coop de la même commune (score nom/adresse/distance, moyenne des composantes disponibles, seuil 83 repris de mednum-cli, Param `seuil_score`) → **418 candidats** en file de revue humaine `dataviz.lieu_appariements_a_valider` (les « ~360 » doublons jamais fusionnés par mednum ; le haut de file = doublons parfaits nom identique/0 m). Invariants : les décisions humaines (`valide`/`rejete`) ne sont jamais écrasées par les runs ; une paire absente du silver n'est pas supprimée (mémoire) ; pas de FK sur lieu_id (survit à la bascule en vue, étape 5) — purge des orphelins par le DAG. Aucun consommateur ne lit encore cette table : aucun impact carte/API à ce stade.

- **`(en cours)` [coop][migration]** — SEPT #1724 étape 2 : `main.personne_affectations_lieu` n'est plus une table répliquée mais une **vue** sur `coop.mediateurs_en_activite` (V151, précédent V144 pour `activites_coop`). La réplique coop-dag (reset global + upsert par run) accusait un lag d'un jour et divergeait : 25 paires actives côté coop absentes, 284 liens morts conservés (utilisateurs supprimés, liens hard-deleted par le job de dédup coop) — 0 divergence d'`est_active` sur les paires communes. Sémantique reproduite : une ligne par (personne, lieu, source='coop'), `est_active` = période non supprimée et non terminée, `created_at`/`updated_at` = min(creation)/max(modification) (utilisés par `api.aidants_connect`), `id` entier déterministe (row_number — aucun consommateur ne le persiste). Les 6 vues dépendantes (liées par OID) sont recréées à l'identique : `api.carto`, `api.aidants_connect`, `dataviz.lieux_inclusion_numerique`/`personne`/`personne_pseudonymisee`/`zonages`. Table gelée en `_legacy` (drop ultérieur), tâche `insert_personne_affectations` supprimée du coop-dag (`drop_working_dir` reprend son rôle de jonction NONE_FAILED). Non-régression validée sur baseline : 4 lignes sur 382, toutes expliquées (liens morts purgés, fraîcheur récupérée). Suivra côté MIN : le modèle Prisma reste lisible tel quel (lecture seule constatée) ; les bases locales min créent encore une table via le schéma Prisma.

- **`(en cours)` [carto]** — SEPT #1724 : garde volumétrique à l'ingestion du fichier national. Un fichier tronqué (publication partielle amont) faisait déréférencer en masse les lieux absents par `integration_lieux` (`visible = FALSE`, carto_id NULL) — le sédiment de ce mécanisme vient d'être nettoyé (1 472 lieux morts supprimés, cf. journal des modifications). Désormais `load_carto_national_file` refuse le run **avant le TRUNCATE du silver** si le fichier contient moins de `seuil × lieux carto en base` (`controler_volumetrie` dans `etl/core/carto.py`, pure et testée ; Param Airflow `seuil_volumetrique`, défaut 0.8, 0 = désactivée, bootstrap base vide jamais bloquant). En cas de refus : la capture bronze du fichier fautif est conservée (traçabilité), le silver du run précédent reste intact, rien n'atteint `main`, et l'échec part en notification Mattermost après les 3 retries.

## 2026-08-18

- **`(en cours)` [personne][migration]** — Successeur strict des DAGs similarities (#1824) : nouveau DAG quotidien `personne-reconciliation` (11:00, après la chaîne d'ingest). Deux tâches : (1) détection des doublons **intra-source** (deux `aidant_connect_id` / `coop_id` / `cn_pg_id` distincts, même nom normalisé, même structure administrative) publiée en photographie TRUNCATE+INSERT dans `dataviz.personne_doublons_intra_source` (V150) — non fusionnables côté entrepôt (comptes recréés à l'import suivant, constaté sur 277/640 fusions de l'ancien DAG), à résoudre à la source ; (2) fusion automatique des seules paires **cross-source strictement complémentaires** : noms identiques après normalisation (pas de fuzzy), affectation emploi active sur la **même SA** des deux côtés (vs « même commune » historique, ~30 % de candidats indécidables en moins), aucun chevauchement d'identifiants (aucune perte possible), paire non ambiguë (personne présente dans une seule paire et dans aucun doublon intra-source). Winner = plus d'identifiants source (égalité : coop_id, puis plus récent) ; mécanique de fusion du curatif du 18/08, journal `audit.personne_merge_log` (`match_type='complementaire_meme_sa'`, dag_id/run_id). Param `fusion_activee` pour repasser en détection seule. Validé sur dataspace_dev : 114 doublons intra-source détectés (78 AC, 29 coop, 7 CN), 14 fusions complémentaires CN↔coop, tous les cas à chevauchement correctement exclus.

- **`(en cours)` [migration]** — SEPT #1828 : `admin.epci.departement_id`, département de rattachement **officiel** des EPCI (V149). Jusqu'ici aucun département unique n'était attribuable à un EPCI : seul le chemin `commune_epci → commune.departement_id` existait, ambigu pour les 91 EPCI interdépartementaux sur 1266 (85 sur 2 départements, 5 sur 3, Métropole du Grand Paris sur 6). Source : **référentiel Banatic (DGCL)** — département de la commune siège, via l'export prégénéré quotidien (xlsx, nouvelle tâche `import_banatic_groupement` d'`init_ref_data` → `import.banatic_groupement`, créée par V149), appliqué par `transfert_epci_departement` après reconstruction de `commune_epci` (`admin.epci` est truncatée à chaque import IGN). Repli pour les EPCI absents de Banatic (millésimes décalés, ex. 4 EPCI fusionnés en 2026 encore dans l'IGN 2025) : département majoritaire en nombre de communes membres, tie-break population puis code département. Backfill embarqué dans V149 (snapshot Banatic 2026-08-18, 1264 paires ; 1266/1266 renseignés dont 4 par repli) car le DAG est manuel. FK vers `admin.departement` (jamais truncatée, ids stables), colonne nullable. La règle majoritaire seule aurait divergé de l'officiel sur 7 EPCI (ex. Grand Paris → 94 au lieu de 75). Suivra côté MIN : miroir du champ dans `schema.prisma`.

## 2026-08-17

- **`(en cours)` [migration][tech]** — N11, point final : V148 droppe `main.structure` et `main.personne_affectations` (28 755 lignes legacy, plus écrites ni lues depuis la bascule de mai — validation 2026-07-29 : 26 592 couvertes par `old_main_structure_id`, 1 330 retrouvées via siret/carto_id/coop_id, 833 départs de flux légitimes, 0 réf `min.*`). Partent avec elles : `main.merge_personne` (`merge_structure` déjà droppée en V144), et les 3 vues hors Flyway `public.geo_struct` / `public.data_errors` / `dataviz.data_errors` (aucun consommateur identifié). DROP **sans CASCADE** : un dépendant inattendu ferait échouer la migration au lieu d'être supprimé en silence. Côté code : `rapport_carto_integration.py` et `rapport_structures.py` basculés sur SA/LI, `test_aidants_connect_ingest.py` adapté, `test_main_structure_constraints.py` supprimé, 3 scripts one-shot archivés (`audit_orphelines_fk`, `dedup_main_adresse`, `generate_carto_orphan_pairs`). ⚠️ Séquencement : merger après !943 (flux débranchés) et !945 (opendata/matviews V145-V147) ; dump prod frais juste avant le merge (irréversible sans pg_restore, cf U148). Suivra côté MIN : retrait des models Prisma `main_structure`/`personne_affectations`.

## 2026-08-13

- **`(en cours)` [coop][migration]** — #1805 : `main.activites_coop` n'est plus une table répliquée mais une **vue** sur `coop.activites` (V144). Cause : la réplication API divergeait de la source — suppressions jamais propagées (`transformer_activites` ignorait `attributes.suppression`, cas constaté lieu 3585 : 207 affiché MIN vs 205 coop), fraîcheur D-1, ~45 600 activités jetées silencieusement (médiateur sans écho `main.personne` — désormais présentes avec `personne_id NULL`). La vue projette les colonnes historiques (labels transformés en SQL — les thématiques perdent leur capitale initiale, choix perf : ~1,5 s au lieu de ~10 s sur les agrégats nationaux MIN, seul Metabase (abandon acté) était sensible à la casse ; `beneficiaires` recalculé identique sur 1998/2000 échantillons) ; jointures `lieu_id` via `lieu_inclusion.structure_coop_id`, `personne_id` via `coop.mediateurs.user_id → personne.coop_id`. Supprimés : chemin ETL activités du `coop-dag` (fetch incrémental, silver `staging.coop__activites`, `ingest_activities`), contrat `coop__activites.yml`, colonne technique `id`, fonction legacy `main.merge_structure` (cassée depuis V084), repointages `activites_coop` des fusions (`merge_personne` + DAGs de similarité — la vue suit `personne.coop_id`). Les 6 vues dataviz dépendantes sont recréées à l'identique. Reste du dead code : branche `coop_activity` du connecteur `http_airflow.py` (à nettoyer dans une MR ultérieure).

## 2026-08-11

- **`(en cours)` [coop]** — Bascule #1707 : décommissionnement des branches SA, personnes et affectations emploi du coop-dag, la coop écrivant désormais directement `main.structure_administrative`, `main.personne` et `main.personne_affectations_emploi` (ADR-002 coop, mergé). Supprimés : `utilisateurs_ingest` (résolution d'identité multi-clés incluse), `force_coordinators_to_mediators` (règle à porter par la synchro coop), l'enrichissement SIRENE + MAJ des SA (`enrich_structures_batch` → `geocode_new_lieux`, BAN only, sélection sur `main.lieu_inclusion`), la branche SA de `ingest_structures` (upserts antenne-aware, résolution dénominations, collisions sans-siret) et la branche emploi de `insert_personne_affectations` (reset `est_active` limité aux affectations lieu). `ingest_structures` sélectionne désormais les lieux par rôle B++ OU présence en LI — plus aucune dépendance aux SA (robuste à la disparition annoncée de `structure_coop_id`) ; effet de bord : les LI des structures mixtes (déjà en SA) sont de nouveau rafraîchis. Conservés : lieux (géocodage BAN cache-first), `build_role_index`, affectations lieu, `coordination_mediation` (rebranchée sur le silver utilisateurs). ⚠️ À ne merger qu'avec la bascule coop déployée (fait, 2026-08) — le connecteur `/api/v1/utilisateurs` reste nécessaire.
- **`(en cours)` [migration]** — V142 : GRANT SELECT/UPDATE sur `coop.activites` et `coop.employes_structures` à `min_scalingo` — la fusion de structures MIN repointe les nouvelles FK coop → `main.structure_administrative` (`structure_employeuse_main_id`, `structure_main_id`).

## 2026-07-30

- **`f9e55ab` [api-carto]** — Fix `courriels` : depuis V089 (22/05) la CTE lisait `li.contact->'emails'`, clé inexistante (les clés réelles du JSONB contact de `lieu_inclusion` sont `telephone`/`courriels`/`site_web`). 100 % des 18 438 lignes d'`api.carto` publiaient `courriels = NULL` alors que ~10 250 lieux ont un courriel. V147 agrège `jsonb_each_text(contact->'courriels')` ; post-fix : 9 361 lignes renseignées.

- **`2c34d2b` [opendata]** — `opendata.lieux_mednum` / `lieux_geojson` basculées sur `main.lieu_inclusion` (V145). Les vues V057 lisaient `main.structure` figée : `opendata-publication` poussait sur data.gouv.fr des données mortes depuis la bascule (la date_maj « fraîche » du legacy n'était que le stamp sirene-backfill). Après : 18 438 lignes synchronisées (vs 15 087 figées), 9 361 courriels (seule la clé générique `email` est publiée), `complement_adresse` exposé. `pivot`/`siret`/`rna` non renseignés depuis #1711 (même choix que V123 pour `api.carto`). V146 droppe les matviews `dataviz.structure_similarities` / `personne_similarities` (détection des DAGs similarities-merge décommissionnés en `f646410`).

- **`f646410` [similarities]** — Décommissionnement de `structures-similarities-merge` et `personne-similarities-merge` (~1 070 lignes) : triggers retirés d'`aidants-connect-dag` (dernier DAG où ils étaient actifs — les fusions étaient relancées chaque jour sur le legacy figé), blocs commentés purgés de carto/coop/idposte. La dédup structures est portée par les contraintes UNIQUE du nouveau modèle (`siret`+antenne, ids source — 0 doublon résiduel mesuré). Côté personnes, pas de clé naturelle cross-source : le reliquat AC ↔ Coop (≤ 875 homonymes candidats ; 844 fusions historiques préservées) est documenté en N13 du plan — détection sur le nouveau modèle + fusion supervisée, à prioriser sur signal métier (labels « Aidant Connect » manquants sur `api.carto`, double comptage stats). Les matviews `dataviz.*_similarities` restent à dropper en migration.

- **`7f3253a` [sirene]** — `sirene-backfill` enrichit `main.structure_administrative` au lieu de `main.structure` (legacy, figée depuis la bascule). Depuis ~2 mois le rattrapage nocturne dépensait le quota INSEE sur la table morte (1 034 lignes re-stampées depuis le 1er juillet) pendant que le backlog réel côté SA (408 lignes dont 141 jamais enrichies) restait en attente. `staging.sirene__etablissements.structure_id` désigne désormais un id de SA (TRUNCATE+INSERT par run, purge auto).

- **`ed213f4` [rapport]** — `rapport_comptage.py` (snapshots avant/après de `ci-cd-dag`) ne compte plus `main.structure` ni `main.personne_affectations` : plus aucun DAG ne les alimente, et leur DROP à venir (N11) aurait fait planter la tâche snapshot de la CI/CD.

- **`231af7d`/`bc06f5f` [aidants-connect][coop]** — Les flux respectent désormais la canonisation (#1681). AC : un nom source qui n'est qu'une redite du `denomination_sirene` du canonique (siret, antenne NULL) s'attache au canonique via la contrainte NULLS NOT DISTINCT au lieu de recréer une antenne doublon. Coop : comparaison au denomination_sirene de la ligne canonique EN BASE (celui fourni par coop est souvent vide) + garde anti-réécriture dans le DO UPDATE structure_coop_id (une antenne NULL de SA siretée n'est plus re-remplie — cause du re-remplissage de ~17 SA constaté entre le 07/07 et le 27/07). idPoste audité : lecture seule, pas de correctif. Tests : 4 cas couverts (casse, non-réécriture, redite→canonique, nom distinct conservé).

Journal au fil de l'eau des évolutions du dataspace (sources, ETL, API, vues, rapports). Lecture orientée dev/agent : contient le hash, les détails techniques et le contexte métier.

Pour la version non-tech (lisible par PO, partenaires, équipe métier), voir [`CHANGELOG-metier.md`](CHANGELOG-metier.md).

## Format

- Ordre chrono inverse (le plus récent en haut).
- Sections par date `## YYYY-MM-DD`.
- Une entrée par MR / commit non-trivial : `- **\`<hash>\` [tag]** — Court titre. Détail.`
- Tags : `[carto]`, `[coop]`, `[aidants-connect]`, `[personne]`, `[api-carto]`, `[rapport]`, `[migration]`, `[tech]`…
- Pour les détails sur quand écrire et comment, voir `CLAUDE.md` section *Documentation des changements*.

---

## 2026-07-31

- **`xxxxxxx` [aidants-connect]** — Silver aidants re-filtré en DELTA : comportement d'ingestion iso pré-fusion, fetch complet conservé. Le premier run post-fusion a échoué sur `ingest_structures` (`UniqueViolation: structure_administrative_siret_antenne_ukey`, clé `(null, HABITAT JEUNES CANTAL)`) : le gate lazy, évalué sur les 18 540 aidants du stock complet au lieu du delta, a réveillé 24 structures dormantes dont un doublon source AC homonyme sans siret — l'ON CONFLICT de l'INSERT SA ne couvre pas la ukey `(siret, denomination_antenne) NULLS NOT DISTINCT` (BUG LATENT CONNU, documenté dans `etl/load/aidants_connect.py`, NON corrigé — fix à décider séparément : dédup Python + quarantaine). Mitigation : `stage_aidants` filtre le transformé sur `updated_at_ac >= MAX(main.personne.updated_at_ac)` (même critère que l'ex-fetch incrémental `?updated_at__gte=`) avant le TRUNCATE+INSERT de `staging.ac__aidants` → tout l'aval (ingest_users, affectations, gate lazy des 2 tâches structures) retrouve le comportement pré-fusion à l'identique. Inchangés : bronze (stock complet capturé), accompagnements (`stage_accompagnements` lit `source.ac__aidants` directement — consolidation quotidienne complète conservée). Limite ré-assumée (iso avant) : un `updated_at` source défaillant refait rater des modifications en gold. Contrats `ac__aidants`/`ac__structures` réalignés.
- **`xxxxxxx` [aidants-connect]** — Fusion aidants + accompagnements : fetch COMPLET quotidien, DAG mensuel supprimé. Le fetch aidants d'`aidants-connect-import` passe de l'incrémental (`?updated_at__gte=MAX(updated_at_ac)`, delta 3–682 lignes/run) au stock complet (endpoint nu, ~18 500 aidants/run) — tâches `get_max_updated_at_ac` + `build_aidants_endpoint` supprimées. Les tâches `stage_accompagnements`/`load_accompagnements` de l'ex-DAG mensuel `aidants-connect-accompagnements` (supprimé) sont rapatriées dans le DAG quotidien : une seule capture `source.ac__aidants` (source_key = endpoint nu, unique) alimente les deux consommations. Effets : consolidation de `main.ac_accompagnements_mensuels` QUOTIDIENNE (l'hypothèse fragile « ≥ 1 snapshot réussi / 6 mois sinon perte définitive des mois sortis de la fenêtre » devient quasi sans objet) ; modifications non ratables même si `updated_at` source défaille ; disparitions d'aidants observables d'un run à l'autre dans le bronze ; gate lazy structures désormais évalué sur le stock complet d'aidants (légèrement plus créateur, conforme à l'intention). Coût assumé (décision 2026-07-31) : AUCUNE rétention implémentée — `source.ac__aidants` (append-only) croît désormais de ~18,5k lignes/jour (~8,2 GB/an, ×70) ; la politique de rétention est documentée dans `approche-data/19-retention-bronze.md`, son implémentation est une décision d'équipe à prendre séparément. Ingests personnes/affectations inchangés (upserts idempotents, ~18,5k lignes/run au lieu du delta). Preuves : ruff + AIR30 clean, 240 tests unitaires verts, contrats ac__aidants/ac__accompagnements réalignés, fiches 15/18/19 à jour.

## 2026-07-30

- **`xxxxxxx` [aidants-connect]** — Quarantaine étendue, MR C/3 (fiche 03) — le chantier « fin des drops silencieux » est clos : les items sans id du snapshot mensuel des accompagnements sont routés vers `staging.rejets`. `transformer_accompagnements` (`etl/core/ac.py`) retourne désormais `(lignes, rejets)` comme le core carto — un item de la fenêtre glissante sans `id` devient un `Rejet` (motif `item_sans_id`, payload = item brut) au lieu d'un `continue` muet ; les mois sans valeur restent ignorés (sparsité normale de la fenêtre, pas un rejet qualité — décision périmètre 2026-07-30). Shell : `stage_accompagnements` (aidants-connect-accompagnements-dag) route via `make_rejets_sink` (flux `ac__aidants`, etape `stage_accompagnements`, même connexion, après le commit du silver). Preuves : ruff clean, mypy core clean, 240 tests unitaires verts (test item_sans_id réécrit en rejet), DagBag conteneur OK (4 tâches), contrat ac__aidants annoté. Les 3 MR du périmètre validé sont livrées ; restent hors quarantaine (filtres métier assumés) : filtre carto coop, gate lazy AC, mois sans valeur AC.
- **`xxxxxxx` [coop]** — Quarantaine étendue, MR B/3 (fiche 03) : les 8 skips silencieux de l'ingest coop sont routés vers `staging.rejets`. `structures_ingest` (etape `ingest_structures`, flux `coop__structures`) : `structure_sans_coop_id` (structure consolidée sans identifiant) et `orpheline_sans_role` (structure ni SA ni LI en base et sans rôle connu du role_index — skip B++ anti-orphelines, ~170 au dernier run). `insert_personne_affectations` (etape `personne_affectations`, flux `coop__utilisateurs`) : `affectation_personne_introuvable`, `affectation_sans_struct_coop_id`, `affectation_emploi_sans_sa`, `affectation_lieu_sans_li`, `affectation_type_inconnu` — payload = affectation brute + coop_id. `insert_coordination_mediation` (etape `coordination_mediation`) : `coordination_mediateur_introuvable` et `coordination_coordinateur_introuvable` (ce dernier n'était même pas compté : `continue` nu — rejeté uniquement si l'utilisateur déclare des coordinations). Compteurs et logs existants conservés (pas de refacto legacy, fiche 16) ; routage via un helper `_quarantaine` (connexion dédiée, sink best-effort `make_rejets_sink`, jamais bloquant). Preuves : ruff clean, mypy core clean, 240 tests unitaires verts, DagBag conteneur OK (coop-import 26 tâches). Reste : MR C (items AC sans id).
- **`xxxxxxx` [tech]** — Quarantaine étendue, MR A/3 (fiche 03, étape 2 fiche 08) : les géocodages invalides sont routés dans `staging.rejets` — premier drop silencieux tracé hors carto. Jusqu'ici, un géocodage écarté (score < 0.5, aucun résultat BAN, code INSEE discordant, réponse illisible) n'était ni caché ni tracé : la structure partait sans adresse BAN, invisible (~3 % des envois API, 12/404 sur le run coop du 2026-07-30 — le nouvel écart du lot 2 caches). `GeocodeurAvecCache` accepte un `rejets_sink` optionnel (pattern `make_rejets_sink`, best-effort, jamais bloquant) : après chaque appel API, les lignes `geocodage_valide=False` deviennent des `Rejet` via le nouveau core pur `rejets_geocodage` (`etl/core/enrichissement.py`) — motif `geocodage_score_faible` si un score existe, `geocodage_sans_resultat` sinon (absence / mismatch INSEE / illisible, indistincts car le core BAN annule le score), payload = clé exactement soumise à l'API (adresse, code_insee, code_postal) + score/label, source_key = id_source. Branché chez les 3 appelants : `coop-dag` (flux `coop__structures`), `aidants-connect-dag` (`ac__structures`), `schema-idPoste` (`idposte__structure`), etape `enrichissement`, même connexion que la capture source. Cache hit = pas de rejet (seuls les appels API réels sont jugés). Sortie du wrapper inchangée. Preuves : ruff clean, mypy core clean, 240 tests unitaires verts (6 nouveaux), DagBag conteneur OK (3 DAGs), contrats ban/coop/ac/idposte annotés. Restent : MR B (skips ingest coop) et MR C (items AC sans id).
- **`xxxxxxx` [coop][tech]** — Lot 2 caches, MR 3/3 (coop) : `new_structures_enriched.csv` + XCom supprimés, l'état enrichi = silver ⋈ caches — le lot 2 est clos, plus aucun CSV enrichi ni XCom d'état dans les 3 flux enrichisseurs. `enrich_structures_batch` garde son rôle d'écrivain (remplissage des caches V137 cache-first, batch UPDATE SIRENE des SA existantes via `last_sirene_enrich_at`) mais ne collecte plus les nouvelles ; `structures_ingest` (tâche `ingest_structures`) re-dérive la sélection depuis le silver (`staging.coop__structures` du run, coop_id absent de `main.structure_administrative` — pas de gate lazy contrairement à AC ; l'enrich ne fait qu'UPDATE, l'ensemble des SA est stable entre les deux tâches) et joint les caches via `lire_cache_sirene`/`lire_cache_geocodage` + le nouveau core pur `consolider_structures_coop` (`etl/core/coop.py` : hit SIRENE par SIRET normalisé, hit géocodage par le triplet (adresse strippée, code_insee source, '') — coop passe le citycode à la BAN, jamais le CP ; consolidation cp/insee priorité géocodage puis source, nom_commune UNIQUEMENT du géocodage — la `commune` source n'est jamais consolidée, iso legacy ; departement non produit car non consommé). `_normalize_structure` (arrays pg, vides→None, ints) reste appliqué aux lignes consolidées — le reste d'`ingest_structures` (lookup/insert adresses, B++ role_index, résolution antennes #1681, upserts SA/LI) est inchangé. Même écart métier que les MR 1/2 (géocodages score < 0.5 non consommés → structure sans adresse BAN, décision 2026-07-30). Preuves : ruff clean, mypy core clean, 234 tests unitaires verts (8 nouveaux `tests-unitaires/coop/test_core_coop_consolidation.py`), DagBag conteneur 25 DAGs OK (26 tâches coop, topologie inchangée — `[enrich, build_role_index] >> ingest_structures` explicite), contrat coop__structures à jour.
- **`xxxxxxx` [aidants-connect][tech]** — Lot 2 caches, MR 2/3 (AC) : `new_structures_enriched.csv` + XCom supprimés, l'état enrichi = silver ⋈ caches. `enrich_structures_batch` garde son rôle d'écrivain (remplissage des caches V137 cache-first, batch UPDATE SIRENE des SA existantes) mais ne produit plus de CSV ; `structures_ingest` re-dérive la sélection des nouvelles structures depuis le silver (`staging.ac__structures` + gate lazy via `staging.ac__aidants` + absentes de `main.structure_administrative` — l'enrich ne fait qu'UPDATE, l'ensemble des SA est stable entre les deux tâches) et joint les caches via `lire_cache_sirene`/`lire_cache_geocodage` + le nouveau core pur `consolider_structures_ac` (`etl/core/ac.py` : hit SIRENE par SIRET normalisé, hit géocodage par le triplet (adresse strippée, '', CP normalisé 5 chiffres — jamais de citycode, le code_insee AC est pollué), consolidation nom_commune/cp/insee priorité géocodage puis source, departement non produit car non consommé). `normaliser_code_postal` extrait en core (réplique `_normalize_cp`, qui délègue). `ingest_structures` prend désormais une liste de dicts (plus de lecture CSV). Même écart métier que la MR 1 (géocodages score < 0.5 non consommés → chemin dégradé adresse brute, décision 2026-07-30). Preuves : ruff clean, mypy core clean (4 annotations manquantes de la MR 1 corrigées au passage), 226 tests unitaires verts (13 nouveaux `tests-unitaires/ac/test_core_ac_consolidation.py`), DagBag conteneur 25 DAGs OK (16 tâches AC, topologie inchangée), contrat ac__structures et docs/aidants-connect.md à jour.
- **`xxxxxxx` [personne][tech]** — Lot 2 caches, MR 1/3 (idposte) : `structure_enriched.csv` supprimé, l'état enrichi = silver ⋈ caches. `enrich_structures_caches` (ex-`get_structure_file_and_process`) relit `staging.idposte__structure` du run et remplit les caches V137 (cache-first, capture bronze SIRENE/BAN conservée) sans plus produire de CSV ; `process_enriched_structure` rejoint le silver aux caches via les nouveaux lecteurs `lire_cache_sirene`/`lire_cache_geocodage` et le core pur `etl/core/idposte.py` (`consolider_structures` : hit SIRENE par SIRET normalisé, hit géocodage par le triplet exact soumis à l'API, consolidation cp/insee priorité géocodage puis source — iso chemin 'base' legacy). Normalisations de clé extraites en core pur `etl/core/enrichissement.py` (`normaliser_siret`, `siret_valide`, `nettoyer_code`, `cle_geocodage`) — `SireneBatch._normalize_siret` et `enrichment_cache._nettoyer_code` délèguent, un seul point de vérité. **Écart métier assumé (décision 2026-07-30)** : les géocodages invalides (score < 0.5) ne sont plus consommés — le cache ne stocke que les valides, donc la ligne sort sans adresse BAN et est filtrée en aval (~2,8 % du volume, 265/9 541 sur base min ; les 130 adresses historiques issues d'un score faible restent en base, non touchées). Nettoyage : `etl/structure_enrichment.py` et `etl/geocoding.py` supprimés (orphelins — seul appelant runtime = idposte, chemin carto mort depuis V136). Restent pour les MR suivantes : AC (`new_structures_enriched.csv` + XCom) puis coop. `last_sirene_enrich_at` gardé tel quel (gate + file backfill, décision 2026-07-30). Preuves : ruff clean, 213 tests unitaires verts (23 nouveaux : enrichissement, idposte, lecteurs cache ; garde-fou pureté AST OK), DagBag conteneur 25 DAGs OK, contrat idposte mis à jour (écart tracé).
- **`xxxxxxx` [migration][tech]** — Caches d'enrichissement SIRENE + géocodage (fiche 05, lot 1 — problème ouvert fiche 08). V137 crée `staging.sirene__cache` (clé : SIRET normalisé 14 chiffres) et `staging.geocodage__cache` (clé : triplet exactement soumis à l'API — adresse construite + citycode + postcode, '' si absents) : référentiels ACCUMULÉS (UPSERT par clé, jamais tronqués — contrairement au silver de flux), fraîcheur par `enriched_at` avec TTL 4 mois appliqué à la lecture (aligné sur le gate `last_sirene_enrich_at` d'AC/coop), aucune FK vers main. Nouveau module `etl/enrichment_cache.py` : wrappers `SireneAvecCache`/`GeocodeurAvecCache` cache-first autour de `SireneBatch`/`GeocodeurBatch` (mêmes signatures, mêmes colonnes de sortie) — lookup préalable, seuls les manquants/périmés partent à l'API, write-through des seuls résultats UTILES (établissement trouvé / géocodage valide ; absents, mismatch INSEE, scores faibles et échecs API re-tentés à chaque run, comme avant), best-effort intégral (erreur SQL avalée → repli sur l'appel API complet, jamais d'interruption de pipeline). Branché dans les 3 flux enrichisseurs : `aidants-connect-dag`, `coop-dag`, `schema-idPoste` (via `write_enriched_data_to_csv`/`_process_base_data_batch`, params `cache_conn`/`cache_run_id`). Au passage, **capture bronze SIRENE branchée chez les 3** (`source_sink_sirene` → `source.sirene__etablissements`, jusqu'ici alimentée par le seul sirene-backfill). Résultat métier iso (le cache restitue exactement ce que produirait l'appel API) ; CSV enrichis et XCom inchangés (leur suppression = lot 2, silver ⋈ caches). Preuves : ruff clean, 190 tests unitaires verts (dont 10 nouveaux `tests-unitaires/enrichment_cache/`), DagBag conteneur OK, V137 appliquée sur base min, contrats sirene/ban annotés (clé `cache:`).
- **`xxxxxxx` [personne]** — Fix UniqueViolation `adresse_code_ban_ukey` sur l'upsert adresse idposte (`process_enriched_structure`, bug latent préexistant révélé au run du 2026-07-30 : code_ban `3650e25c…`, Nîmes). `main.adresse` porte DEUX contraintes uniques — `adresse_ukey` (composite) et `adresse_code_ban_ukey` (code_ban) — mais l'`ON CONFLICT` du batch ne cible que la composite : quand la BAN fait évoluer le libellé d'une adresse (même code_ban, clé composite différente), l'INSERT violait la 2e contrainte et le run échouait. Fix conforme à la règle CLAUDE.md (dédupliquer en Python sur TOUTES les clés uniques avant le batch) : lookup préalable des code_ban du batch déjà présents en base, neutralisation (NULL) des code_ban déjà portés en base ou en doublon intra-batch avant l'INSERT — l'adresse au nouveau libellé s'insère sans code_ban, et la résolution d'`adresse_id` (étape 2, lookup par code_ban) retombe comme avant sur la ligne existante porteuse du code_ban. Couvre aussi le chemin `DO UPDATE SET code_ban = COALESCE(...)` qui pouvait violer la même contrainte. Comportement inchangé hors cas de collision.
- **`xxxxxxx` [carto][migration]** — Silver carto (fiche 01, figuier étrangleur) : le DAG `carto-dag-import` bascule de la table de landing legacy `import.carto` vers `staging.carto__structures`. V136 crée le silver (32 colonnes vivantes + run_id/staged_at, TRUNCATE+INSERT à chaque run, 100 % re-dérivable depuis `source.carto__structures` + `etl/core/carto.py`) et **droppe `import.carto`** avec ses 13 colonnes mortes (adresse_sirene, etat_administratif, code_activite_principale, categorie_juridique, denomination_sirene, 8×ban_* — NULL à 100 % depuis la bascule sur le fichier national), structure_parente (jamais chargée) et **la FK `lieu_inclusion_id` → main, unique lien import → main de toute la base** : le silver reste en sens unique bronze → silver → gold, l'association est déjà donnée par `main.lieu_inclusion.structure_cartographie_nationale_id`. Côté DAG : `truncate_import_before` supprimé (le TRUNCATE vit dans le load) ; `load_carto_national_file` écrit le silver (execute_values, run_id) ; `integration_adresses` perd sa 2e branche ban_* morte et lit staging filtré sur run_id ; `integration_lieux` lit staging (lookup adresse par clé naturelle seule — le lookup clef_interop était mort), perd le backfill `UPDATE import.carto SET lieu_inclusion_id`, et désactive les absents contre le run courant. Nettoyage : `load_carto` (code mort), `etl/transform/reconciliate/merge_files_carto.py` (mort), `scripts/regeocode_orphelines_carto.py` (obsolète) supprimés ; `DTYPE_CARTO` purgé à 32 colonnes ; lecteurs externes repointés (`rapport_carto_integration` : match via lieu_inclusion, `tools/menage-doublons-lieux` : étape re-pointage supprimée, `audit_orphelines_fk` docstring). Logique métier iso : garde de fraîcheur, extraction coop_id composite, étapes 0b/1/1b/2/3, warn coop inconnu inchangés. Preuves : ruff clean, 180 tests unitaires verts, DagBag conteneur OK, V136 appliquée sur base min, contrat carto repointé sur le silver.
- **`xxxxxxx` [migration][tech]** — Silver sirene (fiche 01) : le DAG quotidien `sirene-backfill` matérialise son état transformé, jusqu'ici purement en mémoire (aucun CSV intermédiaire, contrairement aux autres flux). V135 crée `staging.sirene__etablissements`, typée (structure_id BIGINT, siret normalisé, attributs SIRENE, date_creation_sirene DATE, sirene_trouve BOOLEAN), reconstruite à chaque run (TRUNCATE+INSERT). La sortie parsée de `SireneBatch.enrichir_dataframe` est écrite dans le silver puis **relue filtrée sur `run_id`** par l'étape d'UPDATE de `main.structure` (base = interface, fiche 15) — y compris `sirene_trouve` pour le stamp `last_sirene_enrich_at` des SIRET introuvables. `structure_id` (= `main.structure.id`) fait partie de l'état capturé : la sélection du run n'est pas re-dérivable a posteriori (`last_sirene_enrich_at` évolue à chaque run). Tâche unique conservée (pas de split : la sélection ne doit ni passer par XCom ni être recalculée) ; `SireneBatch` (partagé avec les enrichissements carto/idposte, non capturés) intouché ; logique COALESCE/stamp inchangée. Preuves : ruff clean, 180 tests unitaires verts, DagBag conteneur OK, V135 appliquée sur base min, contrat sirene repointé sur le silver.
- **`xxxxxxx` [migration][personne]** — Silver idposte (fiche 01) : le DAG manuel `schema-idPoste` bascule des 6 CSV du working_dir vers le schéma `staging`. V134 crée `staging.idposte__{structure,poste,personne,formation,subvention,contrat}`, typées (BIGINT/DATE/BOOLEAN, JSON contact en TEXT casté à l'ingest), reconstruites à chaque run (TRUNCATE+INSERT), 100 % re-dérivables depuis `source.idposte__conum` + `postes_conum.main` (qui retourne désormais les DataFrames au lieu d'écrire des CSV ; la CLI `__main__` continue d'écrire des CSV). Les 6 lecteurs relisent le silver filtré sur `run_id` en **types natifs** (pas `::text` comme coop) : leurs lookups dict par id (`personne_map`, `structure_map`, `poste_mapping`, clés int venant de la base) exigent des int — des clés str auraient tout fait rater silencieusement (postes à structure NULL, affectations/contrats/subventions skippées). Exception assumée : `structure_enriched.csv` (état POST-enrichissement SIRENE/BAN, non re-dérivable) reste en CSV, rematérialisé depuis `staging.idposte__structure` — problème ouvert documenté dans `approche-data/08` (cible fiche 05 : caches d'enrichissement `staging.sirene_*`/`staging.geocodage_*` adressés par clé d'entrée, chantier dédié). Preuves : ruff clean, 180 tests unitaires verts, DagBag conteneur OK, V134 appliquée sur base min, contrat idposte repointé sur le silver.
- **`xxxxxxx` [migration][tech]** — Silver zonages FRR + QPV (fiche 01) : les deux DAGs mensuels basculent du CSV working_dir vers le schéma `staging`. V132 crée `staging.frr__zonage` (code_insee, type, commentaire), V133 crée `staging.qpv__zonage` (geom_wkt, code, libelle, code_insee, type, source_file) et droppe `import.qpv_staging` (table de chargement créée à la volée par le DAG, plus de producteur). Tables reconstruites à chaque run (TRUNCATE+INSERT), relues filtrées sur `run_id`. Côté DAGs : les transforms écrivent le silver au lieu d'un CSV ; les captures `write_to_source_*` relisent le silver (mêmes records que l'ancien CSV, NULL → '') ; les chargements BashOperator `psql \copy` (FRR : vers `admin.zonage` ; QPV : vers `import.qpv_staging`) sont remplacés par des `SQLExecuteQueryOperator` lisant staging — plus de mot de passe PG injecté dans une commande shell. Bonus QPV : `download_qpv_zip` câblé après `init_working_dir` (race legacy : deux racines parallèles, le download écrivait dans un répertoire que l'init pouvait recréer sous lui). Preuves : ruff clean, DagBag conteneur OK (topologies vérifiées), V132+V133 appliquées sur base min, contrats frr/qpv repointés sur le silver.

## 2026-07-29

- **`xxxxxxx` [coop][migration]** — Silver coop (fiche 01) : bascule complète du flux sur le schéma `staging`, plus de CSV intermédiaires dans le working_dir. V131 crée `staging.coop__structures` (stock du run, hors carto), `staging.coop__utilisateurs` (stock) et `staging.coop__activites` (delta du fetch incrémental), typées, reconstruites à chaque run (TRUNCATE+INSERT), 100 % re-dérivables depuis `source.coop__*` + `etl/core/coop.py`. Côté DAG : les 3 transforms écrivent le staging au lieu de CSVs ; les 6 lecteurs (`enrich_structures_batch`, `utilisateurs_ingest`, `build_role_index`, `insert_personne_affectations`, `insert_coordination_mediation`, `activities_ingest`) relisent le silver en `SELECT col::text` filtré sur `run_id` (protège du gel `freeze_structures` : la table peut garder un run précédent) — mêmes records strings que l'ancien `read_csv(dtype=str)`, corps des lecteurs inchangés, résultat iso. `activities_ingest` lit par curseur serveur (chunks 100k, backfill 3M+ lignes) et conserve le COPY vers la table temporaire. Nettoyage : `dataframe_structures/utilisateurs/activites` supprimés du core (plus de lecteur CSV) avec leurs 12 tests. Hors périmètre (inchangé) : CSV enrichi SIRENE/BAN et role_index.json du working_dir. Preuves : ruff clean, 180 tests unitaires verts, DagBag conteneur OK, V131 appliquée sur base min, contrats coop repointés sur le silver.
- **`xxxxxxx` [aidants-connect][migration]** — Silver AC accompagnements (fiche 01) : le DAG mensuel `aidants-connect-accompagnements` passe sur le même pattern que le flux quotidien. V130 crée `staging.ac__accompagnements` (run_id, aidant_connect_id, mois, nb_accompagnements), reconstruite à chaque run (TRUNCATE+INSERT), 100 % re-dérivable depuis `source.ac__aidants` + le nouveau core pur `transformer_accompagnements` (`etl/core/ac.py`) qui déplie la fenêtre glissante `get_supports_number_last_six_months` en lignes (aidant, mois) — dédup (id, mois) dernière occurrence gagnante, arithmétique de mois sans pendulum (core pur). Côté DAG : nouvelle tâche `stage_accompagnements` (source du run filtrée par `source_key` — évite la collision de run_id avec le quotidien — dédup par id contre les retries → core → staging) ; `load_accompagnements` ne lit plus `source.*` mais le silver, et se réduit à l'upsert `ON CONFLICT (aidant_connect_id, mois)` vers `main.ac_accompagnements_mensuels`. Comportement inchangé (mois du fetch = index 0, mois courant partiel consolidé au snapshot suivant). Preuves : ruff clean, 192 tests unitaires verts (8 nouveaux sur le core), DagBag conteneur OK (fetch → stage → load, contrat en dead-end), V130 appliquée sur base min.
- **`xxxxxxx` [aidants-connect][migration]** — Silver AC quotidien (fiche 01, étape 4 fiche 08) : bascule complète du flux sur le schéma `staging`, en une MR (piège fiche 01 : « silver partiellement en CSV = pire des deux mondes »). V129 crée `staging.ac__aidants` (delta du run — choix requestionnable plus tard) et `staging.ac__structures` (stock complet), typées, reconstruites à chaque run (TRUNCATE+INSERT), 100 % re-dérivables depuis `source.ac__*` + `etl/core/ac.py` — et droppe le landing V095 `import.ac_*` (pseudo-silver tout-TEXT en dead-end, sans lecteur). Côté DAG : les fetchs passent en collecte + capture brute seule (`do_xcom_push` retiré, pattern coop) ; nouvelles tâches `stage_aidants`/`stage_structures` sur le chemin porteur (source du run → core pur, dédup par id contre les retries de fetch → staging) ; les 3 lecteurs basculent de XCom vers staging (`enrich_structures_batch` + gate lazy via `SELECT DISTINCT structure_ac_id`, `ingest_users`, `ingest_personne_affectations`) ; branche `land_*_to_import` supprimée. XCom restants (légitimes) : curseur incrémental `build_aidants_endpoint` et chemin du CSV enrichi SIRENE/BAN (état post-APIs externes, pas un silver re-dérivable — hors périmètre). Annexes : `scripts/backfill_geocodage_ac_cp.py` et les contrats AC repointés sur staging. Preuves : ruff clean, 184 tests unitaires verts, DagBag conteneur OK (16 tâches, deps vérifiées), V127+V128+V129 appliquées sur base min (reset antérieur à V127).
- **`xxxxxxx` [rapport][personne][carto]** — Tableau des règles de survivance (fiche 04, étape 3 fiche 08) : `docs/regles-survivance.md` documente, par entité et par champ, « qui gagne » quand plusieurs sources fournissent la même information — rétro-ingénierie du code réel (les modules `merge.py`/`deduplicate.py`/`reindex.py` cités par la fiche 04 et CLAUDE.md n'existent plus ; la survivance vit dans les upserts d'ingest ON CONFLICT/COALESCE, les gardes de fraîcheur V064/V115/V116, les DAGs similarities V031/V032/V041 et le TRUNCATE idPoste). Constats notables consignés : les DAGs similarities ne sont plus déclenchés que par aidants-connect (triggers coop/carto/idposte commentés depuis la refonte structure) ; les champs SIRENE de `structure_administrative` sont en COALESCE sans garde de fraîcheur (dernier écrivain gagne) ; `sirene-backfill-dag.py` cible encore le legacy `main.structure` ; `adresse` est de fait immuable ; aucune règle d'override humain (une correction manuelle en base peut être écrasée la nuit suivante). Statut : à valider par le métier — règles involontaires marquées ⚠, chantiers ouverts listés (seuils en config #13, crosswalk #12, override_humain).
- **`xxxxxxx` [tech]** — Validation de contrat généralisée aux 11 flux capturés (fiche 02, étape 2 fiche 08) — extension du pilote `ac__aidants` : les 11 contrats `contracts/*.yml` sont validés en mode warn à chaque run, via ~11 tâches dead-end dans 8 DAGs (`coop-dag.py` : structures/utilisateurs/activités après chaque fetch + `ban__adresses` après `enrich_structures_batch` ; `carto-dag-import.py` après `load_data_to_db` ; `aidants-connect-dag.py` : `ac__structures` en plus du pilote ; `aidants-connect-accompagnements-dag.py` : `source.ac__aidants` contre le contrat `ac__aidants` — même endpoint/schéma ; `zonage-frr-dag.py`, `zonage-qpv-dag.py`, `schema-idPoste.py`, `sirene-backfill-dag.py`). Préalable indispensable : réalignement des sections `fields` de 9 contrats sur la forme réellement capturée (dry-run initial : 100 % de bruit) — enveloppe JSON:API par élément `data[]` (coop×3), clés plates du fichier national avec multi-valeurs en chaînes (carto), CSV tout-string (ban, idposte — dont la section fields idposte qui était un mapping non parsable, remplacée par la liste des 80 colonnes avec en-têtes exacts, espaces de bordure quotés compris), capture post-transform (frr, qpv), camelCase INSEE (sirene). Preuve : dry-run sur base min, 11/11 flux conformes, zéro bruit ; DagBag 8/8 OK. Prochaine étape : bascule en bloquant flux par flux après ~2 semaines d'observation sans bruit.
- **`xxxxxxx` [migration][carto]** — Quarantaine `staging.rejets` (fiche 03, étape 2 fiche 08) : V128 crée le schéma `staging` (grants default privileges pattern V002/V004, tables + séquences pour app_python) et la table `rejets` (rejete_at, run_id, flux, etape, motif, source_key, payload JSONB, index `(flux, rejete_at DESC)` pour le suivi du taux de rejet fiche 06). Premiers filtres routés : les 2 drops silencieux de `etl/core/carto.py` — `transformer_lieux` retourne désormais `(df, list[Rejet])` (dataclass pure `etl/core/rejets.py`), motifs `id_trop_long` (> 2000 octets UTF-8) et `code_insee_absent`, payload = lieu brut complet, source_key = colonne `source`. Shell `etl/quarantaine.py` : `make_rejets_sink(conn, run_id, flux, etape)` sur le modèle de `source_capture` (erreurs avalées, jamais bloquant), injecté par `carto-dag-import.py` dans `load_carto_national_file` (flux `carto__structures`, etape `ingest`, même connexion que la capture source). Preuves : 184 tests unitaires verts (5 nouveaux sur les rejets), mypy clean, insertion réelle vérifiée sur base min, DagBag OK.
- **`xxxxxxx` [rapport]** — Tests qualité SQL `tests/quality/` (fiche 03, étape 2 fiche 08) : rétro-ingénierie des 3 rapports manuels en invariants absolus « 0 ligne attendue » — `comptage.sql` (aucune table d'entités `main` vide après un run), `personnes.sql` (`min.personne_enrichie` = `main.personne` ligne à ligne ; domaine fermé `type_accompagnateur` ∈ {mediateur, aidant_numerique}, non NULL), `postes_conum.sql` (témoin Allier par SIRET : existence + totaux figés V1 150 000 € / V2 115 000 € ; domaine `etat` ∈ {rendu, vacant, occupe}). Partage des rôles avec `ci-cd-dag.py` : lui garde les comparaisons avant/après (tolérance 3 % sur base de test restaurée), ces tests portent les invariants absolus exécutables sur la base cible du run, prod incluse. Non repris : les références globales en dur de `rapport_validation.py` (sync 2026-04-14, déjà dérivées : états postes réels 2338/245/2558 vs 2190/228/2721 attendus). Câblage : core pur `etl/core/qualite.py` (parseur des en-têtes `-- test: nom (severite)`, 11 tests TDD, mypy clean) + shell `etl/qualite_tests.py` (exécute chaque invariant via PostgresHook, WARNING par ligne renvoyée, jamais bloquant) + tâche dead-end `tests_main` en fin de `coop-dag.py` (après `drop_working_dir`), sur la base cible du run. Preuve bout en bout dans le conteneur : 6 tests, 0 anomalie sur base min. 179 tests unitaires verts., mode warn (fiche 02, étape 1 fiche 08) — pilote `ac__aidants`. Core pur `etl/core/contrat.py` (TDD, 17 tests) : `parser_champs` (section `fields` des `contracts/*.yml`, types verbeux réduits au 1er mot vérifiable) + `valider_enregistrements` → anomalies agrégées par champ (`champ_requis_absent` — le cas historique `is_visible`, `champ_inconnu`, `type_inattendu` ; bool ≠ integer, null ≠ absent, chemins pointés `organisation.uuid`). Shell `etl/contrat_validation.py` : relit `source.{flux}` du run courant (on valide ce qui est archivé), warnings dans les logs Airflow, jamais bloquant. Tâche dead-end `valider_contrat_aidants` dans `aidants-connect-dag.py` après le fetch. Rejeu réel : 2 derniers runs delta (4 et 15 enr.) conformes, zéro bruit. Au passage : `contracts/ac__aidants.yml` n'était pas du YAML valide (`?` non quoté) — corrigé.
- **`xxxxxxx` [migration]** — Journal des captures `source.capture_run` (V127, fiche 08 étape 1) : 1 ligne par (run Airflow × table `source.*`), ouverte dès la construction du sink avec `nb_lignes = 0` puis cumulée lot par lot (`ON CONFLICT (run_id, table_cible) DO UPDATE`, même transaction que l'insertion des lignes). Distingue « run exécuté, 0 ligne reçue » (légitime sur un flux incrémental comme `ac__aidants`) d'une absence totale de capture (échec silencieux) — les deux cas étaient indistinguables. `parametres` JSONB journalise le contexte du fetch, notamment le curseur incrémental (`APIClientOperator` y écrit son `endpoint`, qui porte l'`updated_at__gte` des aidants) : chaque delta redevient ré-interprétable sans dépendre de l'état de `main`. Erreurs du journal avalées comme celles du sink (la capture ne casse jamais le pipeline). GRANTs hérités des default privileges V099/V114 (vérifié sur `min__evenements`). 10 nouveaux tests (`tests-unitaires/source_capture/test_capture_run.py`), 151 verts.
- **`xxxxxxx` [tech]** — Hygiène 3.4 (`approche-data/17`) : typage progressif — mypy sur `etl/core/` UNIQUEMENT (fiche 16 : le neuf est typé, le legacy jamais). Annotations ajoutées aux 4 modules core (comportement inchangé, 141 tests verts, imports vérifiés dans le conteneur) ; config `[tool.mypy]` dans le pyproject racine (`disallow_untyped_defs`, legacy `etl.transform.*` suivi en Any, pandas sans stubs) ; nouvelle ligne au job CI `test-unitaires` (`uv run --project tests-unitaires -- mypy etl/core`, mypy ajouté à cet env). Clôt l'étape 3 (hygiène) du plan.
- **`xxxxxxx` [tech]** — Hygiène 3.3 (`approche-data/17`) : garde-fou architecture (fiche 16 FCIS) — nouveau test `tests-unitaires/architecture/test_core_pur.py` : les modules d'`etl/core/` n'importent ni airflow, ni psycopg2, ni requests. Vérification statique par AST (pas d'import runtime, le conftest stubbant airflow) et transitive sur les imports internes `etl.*` (core/ac → core/coop → transform/normalizer_utils, chacun vérifié), message d'échec avec chaîne de provenance. Porté par le job CI `test-unitaires` déjà bloquant — aucun changement de `.gitlab-ci.yml`. 141 tests verts.
- **`xxxxxxx` [tech]** — Hygiène 3.2 (`approche-data/17`) : ruff seul en pre-commit — `ruff-check --fix` (F = autoflake, I force-single-line = reorder-python-imports) + `ruff-format` (= black) remplacent les trois hooks historiques ; hook AIR30 dédié conservé (--preview). Config `[tool.ruff]` dans pyproject.toml, reprise par le `ruff check .` de la CI (le tri d'imports y devient contrôlé). Reformatage repo-wide : 66 fichiers (cosmétique — black ne tournait que sur les fichiers touchés), DagBag et 140 tests inchangés. Au passage : 5 contrats YAML corrigés (`[` / `{` non quotés — check-yaml n'était jamais passé dessus en --all-files).

- **`xxxxxxx` [tech]** — Hygiène 3.1 (`approche-data/17`) : `pyproject.toml` racine + `uv.lock` — dépendances épinglées aux versions de la stack cible (Airflow 3.1.6 / python 3.11 : pandas 2.1.4, numpy 1.26.4, sqlalchemy 1.4.54…), groupe dev (pre-commit, ruff). Usage local : `uv sync` / `uv run`. `requirements.txt` reste consommé par la CI (pip-audit, test-dag) et le déploiement serveur — remplacement progressif, ne pas désynchroniser les deux fichiers. `tests-unitaires/` garde son pyproject autonome (Airflow stubbé).

- **`xxxxxxx` [aidants-connect][tech]** — Nettoyage du legacy AC (MR 4, fin de la boucle AC quotidien `approche-data/17`) : `APIClientOperator._transform_data` supprimé (~110 lignes) avec les imports normalizers/pandas devenus inutiles — sans consommateur depuis la bascule (MR 3, validée en local : run vert, landing = capture). Le data_type `aidants-accompagnements` avec `do_xcom_push=True` lève désormais un ValueError explicite (chemin qui n'a jamais eu de consommateur, son seul appelant est en `do_xcom_push=False`). Aucun changement de comportement.

- **`xxxxxxx` [aidants-connect]** — Bascule du fetch AC quotidien sur le core (MR 3 boucle AC, `approche-data/17`) : la boucle de pages de `APIClientOperator.execute` appelle `etl.core.ac.transformer_aidants` / `transformer_structures` sur les `results` de chaque page, au lieu de `_transform_data` + concat DataFrame. Le XCom porte désormais des listes de dicts python (None au lieu de NaN dans les rares champs numériques absents — sans effet aval, tout repasse par `pd.DataFrame`). La branche `aidants-accompagnements` reste sur `_transform_data`, déjà morte (seul appelant en `do_xcom_push=False`) — suppression en MR 4. 0 écart prouvé en MR 2 sur le stock complet réel.

- **`xxxxxxx` [aidants-connect]** — Core AC quotidien en fonctions pures (MR 2 boucle AC, `approche-data/17` ; la MR 1 capture était déjà en place, pattern fiche 15) : `etl/core/ac.py` (`transformer_aidants`, `transformer_structures`) réplique `APIClientOperator._transform_data` pour les data_type `aidants-personnes` / `aidants-structures` (normalisation prénom/nom, `validate_pivot` sur SIRET numérique JSON, dérivation `dispositif_programmes_nationaux` depuis `france_services_label`), fixé par 16 tests `tests-unitaires/ac/`. Réutilise `parse_timestamp`/`to_pg_array` du core coop et les normalizers purs. Aucune correction décidée : rejoué sur le stock complet brut réel du 2026-07-01 (18 539 aidants + 5 865 organisations imbriquées) = 0 écart vs legacy. Code mort à ce stade — rien de branché (bascule en MR 3).

## 2026-07-28

- **`xxxxxxx` [carto]** — Bascule du chargement carto sur le core (MR 3 boucle carto, `approche-data/17`) : `load_carto_national_file` appelle `etl.core.carto.transformer_lieux(lieux, set(DTYPE_CARTO))` ; la transformation inline (regex structure_coop_id, rejets, restriction colonnes) est supprimée dans le même geste — elle était enfouie dans la fonction, bascule et nettoyage confondus. Aucun changement de comportement attendu : core prouvé identique au legacy sur les 18 482 lieux réels (MR 2). Validation par run local du DAG carto.

- **`xxxxxxx` [carto]** — Core carto en fonction pure (MR 2 boucle carto, `approche-data/17`) : `etl/core/carto.py` (`transformer_lieux`) réplique la transformation enfouie dans `load_carto_national_file` (dérivation `structure_coop_id` par regex sur ids coop standalone, rejet ids > 2000 octets, rejet sans code_insee, restriction aux colonnes d'import.carto — injectées par le shell, le core n'importe pas `database_utils` qui tire airflow), fixée par 13 tests `tests-unitaires/carto/`. Aucune correction décidée : comparaison hors-ligne sur les 18 482 lieux réels capturés = identique au legacy (18 438 lignes, 6 102 structure_coop_id, 23 sources, 44 rejets). Code mort à ce stade — rien de branché (bascule en MR 3).

- **`xxxxxxx` [carto][source]** — Capture brute carto déplacée en pré-transform (MR 1 boucle carto, `approche-data/17`). Avant : `write_to_source_carto` capturait POST-chargement (`SELECT * FROM import.carto`) — les lignes rejetées (id > 2000 octets, code_insee absent) n'étaient jamais capturées, et le brut était déjà restreint aux colonnes `DTYPE_CARTO` (+ `structure_coop_id` dérivé). Après : `load_carto_national_file` accepte un `source_sink` optionnel et capture le fichier national juste après `json.loads`, avant tout rejet/dérivation — brut complet, rejets inclus ; source_key = colonne `source` par lieu (inchangé), sink groupé par source_key. La tâche dead-end `write_to_source_carto` et ses imports sont supprimés. Aucun changement sur `import.carto` ni l'aval.

- **`xxxxxxx` [ban][tech]** — Nettoyage du legacy BAN (MR 4, fin de la boucle BAN `approche-data/17`) : suppression de la dataclass `AdresseGeocodee` (et import `dataclass`) dans `etl/geocoding_batch.py`, sans consommateur depuis la bascule (MR 3). Bascule validée en local avant nettoyage : run `coop-import` post-bascule vert, `code_ban` UUID présent sur les adresses créées/mises à jour dans `main.adresse`. Aucun changement de comportement.

- **`xxxxxxx` [ban]** — Bascule du géocodage sur le core (MR 3 boucle BAN, `approche-data/17`) : `GeocodeurBatch._geocoder_batch` appelle `etl.core.ban.transformer_reponses` au lieu de sa boucle de parsing inline ; la conversion NaN→None (forme source) sert à la fois la capture brute et l'entrée du core ; le chemin d'erreur par batch utilise `ligne_vide`. **Effet métier à la bascule** (fix MR 2 actif) : `code_ban` enfin renseigné côté géocodage (lecture de `result_banId`, casse réelle de l'API — avant : 100 % None). Sur l'échantillon local : 2 119 code_ban sur 2 320 réponses. `AdresseGeocodee` et sa construction n'ont plus de consommateur dans le chemin nominal (suppression en MR 4).

- **`xxxxxxx` [ban]** — Core BAN en fonctions pures (MR 2 boucle BAN, `approche-data/17`) : `etl/core/ban.py` (`transformer_reponses`, `ligne_vide`) réplique la transformation enfouie dans `GeocodeurBatch._geocoder_batch` (rejet sur mismatch INSEE, parsing coordonnées/score, seuil de validité, geom WKT), fixée par 19 tests `tests-unitaires/ban/` sur payloads conformes au brut réel de `source.ban__adresses`. **Bug legacy décidé et corrigé dans le core** : `code_ban` lisait `result_banid` (minuscules) alors que l'API renvoie `result_banId` — clef inexistante, `code_ban` 100 % None côté géocodage depuis toujours. Chiffré en rejouant le core sur les 2 320 lignes réelles capturées : 2 119 `code_ban` renseignés (avant : 0), 2 262 géocodages valides, 38 rejets INSEE. Code mort à ce stade — rien de branché, `GeocodeurBatch` inchangé (bascule en MR 3).

- **`xxxxxxx` [ban][source]** — Capture brute BAN enfin branchée (MR 1 boucle BAN, `approche-data/17`). Le sink `source.ban__adresses` était câblé dans `GeocodeurBatch` (capture du CSV réponse complet de l'API Géoplateforme, avant le filtre INSEE/score) mais aucun appelant ne le passait — table vide depuis sa création (0 ligne, 0 run, cf. `contracts/ban__adresses.yml`). Désormais passé par les 3 appelants : `enrich_structures_batch` de coop-dag et d'aidants-connect-dag, `write_enriched_data_to_csv(…, "base")` de schema-idPoste (connexion dédiée, commit par batch, run_id Airflow, source_key = URL de l'endpoint). Fix au passage dans `GeocodeurBatch._geocoder_batch` : conversion NaN pandas → None avant `json.dumps` (les cellules vides du CSV réponse produisaient un littéral `NaN` invalide en JSONB — la capture échouait silencieusement). Validé en local : run `coop-import` du 2026-07-28, 2 320 lignes capturées, payload JSONB propre. Permet d'auditer a posteriori un géocodage douteux (score, label retenu, rejets silencieux sur mismatch INSEE). Aucun changement sur les données publiées.

- **`xxxxxxx` [coop][tech]** — Nettoyage du legacy coop (MR 4, fin de la boucle coop `approche-data/17`) : suppression dans `APIClientOperator` des branches coop de `_transform_data` (activités, utilisateurs, structures), des helpers coop-only (`normalize_labels`, `format_as_label`, `build_contact`, `_normalize_json_str`), de l'écriture CSV coop et du paramètre `chunk_size` — 539 lignes supprimées, sans consommateur depuis la bascule (MR 3). `_to_pg_array`/`parse_timestamp` conservés (utilisés par les flux aidants-*), pagination coop `links.next.href` conservée (capture brute). Bascule validée en local avant nettoyage : run `coop-import` complet vert, 12 664/6 152/11 604 objets bruts capturés, labels corrigés vérifiés en base (« Creer avec le numerique »). Aucun changement de comportement.

- **`xxxxxxx` [coop]** — Bascule du flux coop sur la couche source + core (MR 3 boucle coop, `approche-data/17`). Avant : les fetch transformaient en mémoire (`_transform_data` enfoui dans `APIClientOperator`) et écrivaient les CSVs consommés par l'aval. Après : les fetch se limitent à la collecte + capture brute (`do_xcom_push=False`) ; trois tâches `transform_{structures,utilisateurs,activites}` lisent le brut de `source.coop__*` (run courant, dédup par id — un retry de fetch ne produit plus de doublons), appellent le core pur (`etl/core/coop.py`) et écrivent les MÊMES CSVs (noms, colonnes, sérialisation JSON, Int64) — l'aval (ingest, role_index, enrichissement SIRENE/BAN) est inchangé. **Effets métier à la bascule** (les 2 fix du core prennent effet) : `deleted_at_coop` structures enfin renseigné, labels à majuscules internes non mutilés — à chiffrer par `rapport_comptage.py` avant/après. Le legacy `_transform_data` coop n'a plus de consommateur (suppression en MR 4).

- **`xxxxxxx` [coop][tech]** — Core FCIS coop complet (MR 2c boucle coop, `approche-data/17`) : `etl/core/coop.py::transformer_activites` — fonction pure brut `source.coop__activites` → lignes activités, contrat fixé par 19 tests (60 au total sur `tests-unitaires/coop/`). Comportement identique au legacy `_transform_data` (date tronquée à 10 car., dates creation/modification transmises brutes, matériel non re-labellisé, beneficiaires en JSON compact, `oriente_vers_structure` défaut False) SAUF **fix labels à majuscules internes** : la majuscule interne est séparée par un espace au lieu d'être supprimée avec sa lettre (legacy `normalize_labels`/`format_as_label` : « CreerAvecLeNumerique » → « Creerveceumerique » ; désormais « Creer avec le numerique ») — zéro écart sur les labels snake_case observés. Concerne `thematiques`, `autonomie`, `structure_de_redirection`. **Code mort à ce stade** : rien de branché avant la bascule (MR 3), où l'écart sera chiffré.

## 2026-07-27

- **`xxxxxxx` [coop][tech]** — Naissance du core FCIS coop (MR 2a/2b boucle coop, `approche-data/17`) : `etl/core/coop.py::transformer_structures` et `::transformer_utilisateurs` — fonctions pures brut `source.coop__*` → lignes finales, contrat fixé par 41 tests unitaires (`tests-unitaires/coop/`). Comportement identique au legacy `_transform_data` (structures : filtres carto, pivot siret/rna, contact JSON, pg-arrays avec virgules supprimées, `or None` sur compteurs ; utilisateurs : contact dict `{"coop": …}`, is_visible sous `mediateur.is_visible`, affectations portant `fin`, emplois exclus si CN complet, coordination_mediation) SAUF **fix `deleted_at_coop` structures** : lit `attributes.suppression` (legacy : `parse_timestamp("suppression")` chaîne littérale → 100 % NULL sur 162 948 lignes ; le sous-flux utilisateurs n'a pas ce bug). **Code mort à ce stade** : rien n'est branché, aucun impact prod tant que la bascule (MR 3) n'a pas eu lieu ; l'effet du fix sera chiffré à la bascule.

- **`xxxxxxx` [coop][source]** — Capture couche `source` réellement brute pour coop-numerique (même correction que aidants-connect le 2026-07-22, MR 1 de la boucle coop du plan `approche-data/17`). Avant : `source.coop__{structures,utilisateurs,activites}` alimentées depuis le CSV **post**-`_transform_data()` (booléens en strings, structures imbriquées re-sérialisées, thematiques en syntaxe pg-array, champs API non mappés perdus, structures filtrées carto absentes). Après : `source_table` + `source_db_conn_id` sur les 3 tâches fetch de `APIClientOperator` — capture des objets bruts page par page dès réception, avant transformation ET avant filtres (le flux structures capturé est désormais complet, y compris les ~visibles carto exclues en aval). Tâches `write_to_source_*` et helper `_write_csv_to_source` supprimés. Lignes antérieures = forme transformée, nouvelles = brut API (`attributes.*`) ; discriminer par la présence de la clef `attributes`. Comportement aval (`import`/`main`) inchangé.

- **`xxxxxxx` [tech]** — Socle qualité : 12 contrats de flux documentaires (`contracts/`), doctrine bronze d'abord + architecture FCIS + plan de remise au propre dirigé par les tests (`approche-data/15-17`), dossier `tests-unitaires/` (purs, uv autonome, Airflow stubbé) avec 10 tests de la capture brute (`etl/source_capture.py`), job CI `test-unitaires` bloquant sur chaque MR.

## 2026-07-24

- **`xxxxxxx` [carto][coop]** — Garde de fraîcheur sur `main.lieu_inclusion` : un lieu n'est mis à jour que si la date de màj annoncée par la source est STRICTEMENT plus récente que `updated_at` global (GREATEST carto/coop/min, V116) — remplace la détection de diff de contenu champ à champ (`_CARTO_CHANGED_WHERE` carto, WHERE du `ON CONFLICT` coop). Côté carto : `import.carto.date_maj` (jusqu'ici ignorée) ; côté coop : champ `modification` de l'API (`updated_at_coop`, déjà extrait pour les SA, désormais passé aux lieux). `updated_at_carto`/`updated_at_coop` stockent la date source (plus `now()`) → la fraîcheur exposée (`date_maj` d'api.carto) reflète le vrai changement métier côté source. Date source absente → pas de preuve de fraîcheur → ligne ignorée (lieu existant) ; nouveau lieu inséré avec colonne source NULL (`date_maj` retombe sur `created_at`). Effet principal : une édition MIN ou une source plus récente n'est plus écrasée par un flux périmé. Hors garde (cycle de vie/lien, toujours appliqués) : rattachement du `carto_id` aux lieux matchés par coop_id, transferts de coop_id, réactivation `visible_pour_cartographie_nationale` des lieux présents dans le flux, désactivation des absents. Dédup coop amont : garde l'occurrence au `updated_at_coop` le plus récent (avant : dernière occurrence).

## 2026-07-22

- **`xxxxxxx` [aidants-connect][migration]** — Historique mensuel des accompagnements par aidant. L'API AC ne renvoie qu'une fenêtre glissante de 6 mois (`get_supports_number_last_six_months`, index 0 = mois courant … 5 = M-5) : nouveau DAG `aidants-connect-accompagnements` (cron `0 6 1 * *`, le 1er de chaque mois à 06:00, sans notification Mattermost) qui fetch le stock complet des aidants (data_type `aidants-accompagnements` dans `APIClientOperator`, extraction id + fenêtre) et upsert `(aidant_connect_id, mois, nb_accompagnements)` dans `main.ac_accompagnements_mensuels` (V126 : PK composite, CHECK mois = 1er du mois, `fetched_at`). `ON CONFLICT (aidant_connect_id, mois) DO UPDATE` : les 5 mois de recouvrement entre snapshots sont consolidés avec la valeur la plus fraîche (le mois courant, partiel au fetch, est finalisé par le snapshot suivant) → série temporelle complète au fil des runs. Le mois est calculé à la date d'exécution réelle (pas la date logique Airflow), un run manuel en cours de mois reste correct. Capture brute couche source du snapshot complet dans `source.ac__aidants` (source_key = endpoint sans filtre, distinct des deltas incrémentaux). `U126` = DROP TABLE (détruit l'historique, non reconstructible).

- **`xxxxxxx` [aidants-connect][source][tech]** — Capture couche `source` réellement brute pour Aidants Connect. Avant : `source.ac__structures` / `source.ac__aidants` étaient alimentées depuis le XCom des tâches fetch, donc **après** `_transform_data()` (champs renommés `first_name`→`prenom`, `uuid`→`structure_ac_id`…, valeurs normalisées, champs API non mappés perdus) — contrairement à l'intention "aucune transformation" de la couche source. Après : `APIClientOperator` accepte `source_table` + `source_db_conn_id` (templaté) et capture les objets bruts des pages JSON (`results`/`data`) juste après `_fetch_all_pages()`, avant toute transformation, via le sink tolérant aux erreurs de `etl/source_capture.py` (append-only, un échec de capture n'interrompt jamais le fetch). Tâches DAG `write_to_source_ac_structures`/`write_to_source_ac_aidants` et helper `_write_records_to_source` supprimés (remplacés par la capture in-operator). Conséquence : les lignes `source.ac__*` antérieures contiennent les clefs transformées, les nouvelles les clefs API d'origine (`source_key` = endpoint, inchangé ; fetch aidants toujours incrémental → delta du run).

## 2026-07-21

- **`V125` [migration][tech]** — Événements MIN : clef `actor` renommée `user_id` dans `donnee` (`source.min__evenements`), plus parlant. Commentaire de colonne mis à jour ; pas de reprise des lignes existantes — l'app MIN émet `user_id` à partir du déploiement correspondant. `U125` = renommage inverse.

## 2026-07-17

- **`V124` [migration][tech]** — Capture des modifications MIN dans la couche source : `source.min__evenements`, structure canonique V099 (`id`/`run_id`/`ingested_at`/`source_key`/`donnee` jsonb), append-only. MIN devient une source parmi les autres — seule table du schéma `source` dont le producteur n'est pas l'ETL. `donnee` = événement complet (`action` create/update/delete, `entity_id`, `actor`, `value` : create/delete = snapshot, update = `{"old","new"}` limités aux propriétés modifiées) ; `source_key` = entité modifiée ; `run_id` = identifiant de corrélation app (pas Airflow). Pas de CHECK : invariants garantis par l'app MIN comme pour toute source externe. Un index d'expression `(source_key, donnee->>'entity_id', ingested_at DESC)` pour l'historique d'une entité. Grants : hérités des default privileges (`app_python` CRUD V099, `min_scalingo` SELECT V114 conservé) + `GRANT INSERT` et USAGE séquence explicites à `min_scalingo` (producteur). `U124` = DROP TABLE (détruit l'historique).

## 2026-07-15

- **`V123` [migration][api-carto][tech]** — Suppression de la table d'association lieu ↔ structure administrative (`main.lieu_inclusion_structure_administrative`, ticket SEPT #1711). Lien lâche et de mauvaise qualité (7 sources d'écriture non qualifiées, ~50 % de lignes non corroborées, stock SIRET gelé `V093_siret_bridge` arbitraire sur les collisions d'antennes AC), jugé non indispensable après validation PO — les données régaliennes (financements, conventions) vivent sur SA + postes, jamais sur ce rattachement. Côté application, MIN a cessé de le lire (PR min#1714, déployée avant cette migration). Réécriture des 8 objets live qui joignaient l'asso pour qu'ils n'en dépendent plus (coupe franche, pas de repli via `structure_coop_id`), puis `DROP TABLE` : `api.carto` + `api.get_carto_mediateur` (`pivot` SIRET/RNA → fallback `00000000000000`, 2 131 lieux concernés, aucune ligne perdue) ; `api.structures` + `dataviz.structures` (la branche appariée SA×LI disparaît → deux listes disjointes : toutes les SA + tous les LI, sans appariement — `api.structures` passe de ~30 k à 36 242 lignes = 11 576 SA + 24 666 LI) ; `api.aidants_connect` (l'adresse du cas emploi retombe sur celle de la SA employeuse) ; `dataviz.lieux_inclusion_numerique` (colonnes SIRENE siret/rna/catégorie juridique/état → NULL) ; `dataviz.structures_employeuses` (`nombre_accompagnements_médiateurs_numériques`, `nombre_de_médiateurs`, `est_france_services` → vides) ; `api.get_mediateur` (siret + contacts des lieux d'activité → NULL / `[]`). `coop-dag.py` : retrait de la tâche `batch_upsert_asso_*` (seul producteur runtime de l'asso). Scripts nettoyés : `rapport_comptage.py`, `rapport_structures.py`, `supprimer_lieux_inclusion_orphelins.py`. Casts `NULL` des branches UNION typés enum (post-V122) ; `formations_labels::text[]` conservé. Vérifié sur `dataspace_dev` (apply V121→V122→V123) : table absente, aucune vue/fonction ne référence plus l'asso, les 6 vues renvoient des lignes, les 2 fonctions s'exécutent sans erreur ; `U123` (rollback : recrée la table vide + restaure les définitions avec-asso) exécutable en transaction. Refs #1711.

## 2026-07-08

- **`(en cours)` [carto]** — Fin des doublons de lieux créés par les fusions mednum-cli (min#1585). Quand mednum-cli fusionne un lieu Coop numérique avec une autre source, l'id devient composite (`Coop-numérique_<uuid>__France-Services_789`) et `structure_coop_id` arrive vide dans le flux → le match par coop_id de `integration_lieux` échouait et un doublon était inséré : fiche carto **visible et vide** + fiche coop **cachée** portant les activités (cas type : « FS Castillon-la-Bataille », 505 activités sur la fiche cachée). Mesuré base dev : **1 424 paires** (557 291 activités toutes portées par les fiches cachées, 0 par les visibles), sources fusionnées : RhinOcc 431, Dora 405, France-Services 308… Correctif : dans le CTE `incoming`, `COALESCE(structure_coop_id, UUID extrait de l'id par regexp)`. Extraction **stricte** : les ids mednum-cli sont des segments séparés par `__`, seul un segment exactement `Coop-numérique_<uuid>` (début d'id ou précédé de `__`) est un id coop — `Numi_Coop-numérique_<uuid>` est un id **Numi** (le hub normand dérive ses ids des uuids coop), jamais extrait. Au plus un segment coop par id (vérifié sur le dernier run source). 16 lieux en base portent un id carto Numi avec coop_id : liens légitimes posés par le DAG quand le flux fournissait `structure_coop_id` (~56 runs historiques), non touchés. Rattrapage : outil `tools/menage-doublons-lieux/` (uv, dry-run par défaut, rapport Markdown des fusions en guise de trace) fusionnant les paires existantes (transfert carto_id + associations SA vers la fiche coop, suppression de la fiche vide) — **à exécuter avant le premier run du DAG corrigé**, sinon l'étape 0b transfère le coop_id sur la fiche vide. 2 paires exclues (fiche coop portant déjà un autre carto_id = doublon côté cartographie nationale, à traiter en amont).

- **`V121`/`V122` [migration][coop][carto]** — Évolutions de `main.lieu_inclusion` pour l'intégration coop (#1700). **`V121`** : trois colonnes portées par `coop.lieu_inclusion` et absentes de l'Entrepôt — `complement_adresse` (sur le lieu et PAS sur `main.adresse`, normalisée BAN et mutualisée : 3 744 adresses référencées par plusieurs lieux, 2 424 partagées SA↔lieu), `siret_a_l_enrichissement` (SIRET **déclaratif** saisi à la création côté coop, entrée du pipeline d'association lieu↔SA ; le SIRET canonique reste sur la SA, doctrine V069), `nom_usage`. **`V122`** : les 10 colonnes métier `TEXT[]` (`typologies`, `services`, `modalites_acces`…) deviennent des **enums Postgres** créés dans `main` (propriété Flyway), valeurs **verbatim** des `@map` Prisma coop (accents, « », apostrophe U+2019 dans « Ce lieu n’accueille pas de public ») — `autres_formations_labels` reste `TEXT[]` (texte libre). Au passage : nettoyage de 11 lignes polluées par des listes Python stringifiées (`['Gratuit']`…, bug de `_normalize_array` dans `coop-dag.py` corrigé dans la même MR via `ast.literal_eval`), normalisation des alias hors référentiel écrits historiquement par l'UI d'édition MIN (« Se présenter sur place » → « Se présenter », « Atelier collectif » → « Dans un atelier collectif » ; UI corrigée dans la PR min associée), `main.sorted_text_array` passe de `text[]` à `anyarray` (les colonnes enum n'ont pas de cast implicite vers `text[]`), recréation des 6 vues dépendantes (`api.carto`, `dataviz.structures` — `NULL::text[]` des branches UNION typés enum —, `dataviz.lieux_inclusion_numerique` — concat `formations_labels::text[] || autres_formations_labels` —, `dataviz.personne`, `dataviz.personne_pseudonymisee`, `dataviz.structures_employeuses`), et casts `::main.<enum>[]` sur les écritures de `coop-dag.py` / `carto-dag-import.py` (psycopg2 adapte les listes en `text[]`, refusé sans cast explicite ; une valeur hors enum fait désormais échouer le run — fail-fast voulu). ⚠️ **Coordination requise avant apply** : `min/prisma/schema.prisma` (`main_lieu_inclusion`, colonnes en `String[]`) et `coop apps/web/prisma/entrepot/schema.prisma` (idem) cassent à la lecture de ces colonnes tant que leurs mappings ne passent pas aux enums — `apply_migration_dev` étant automatique au merge, synchroniser avec les deux équipes. Vérifié sur copie de dataviz_test (37 312 lieux) : cast intégral OK, comptages des 6 vues identiques, chemins d'écriture des 2 DAGs rejoués. Refs #1700.

## 2026-07-06

- **`V119`/`V120` [migration]** — Flags `est_grand_reseau` et `est_hub` sur `main.structure_administrative`. Grand réseau = SIREN avec structures dans ≥ 3 départements hors collectivités (détection auto : 30 réseaux / ~918 structures — Croix-Rouge, La Poste, Orange…). Hub = tête de réseau régionale de la médiation numérique (seed référentiel Hubs_SIRET : 15 SIREN / 30 structures, porteurs de consortium inclus). Objectif : exclure ces structures des fusions/rapprochements automatiques (#1681). Peuplement : scripts/flagger_grands_reseaux.sql et scripts/flagger_hubs.sql (idempotents).

- **`(en cours)` [carto]** — Suppression de la tâche `delete_paca_paris_ids` de `carto-dag-import` qui recréait chaque nuit tous les lieux Paca/Paris. La tâche (introduite `ecb8ee2`/`bdd25e4`, janv. 2026, pour contourner une supposée instabilité des ids mednum-cli de ces sources) mettait `structure_cartographie_nationale_id = NULL` + `visible = FALSE` sur tous les lieux `source IN ('Paca','Paris')` juste avant `integration_lieux`. Sa ré-association implicite reposait sur le matching `(siret, nom, adresse)` supprimé par la refonte phase 4 (`c454812`, mai 2026) : depuis, seuls carto_id/coop_id matchent, et 100 % des lieux Paca/Paris actifs n'ont pas de coop_id → **454 lieux recréés à chaque run** (130 Paca + 324 Paris), soit ~14 670 lignes orphelines invisibles accumulées en base dev depuis le 2026-05-26 (ex. « Le 264 » : 33 copies ; « Les Astroliens » : 264). Effets de bord : ids `main.lieu_inclusion` instables pour les consommateurs, liens perdus (`activites_coop`, `personne_affectations_lieu`, `lieu_inclusion_structure_administrative` pointant sur des orphelins). Vérifié via `source.carto__structures` (mars→juillet) : les ids Paca/Paris sont **stables** entre imports (438/438 ids communs conservés, seules retouches de formatage de noms) → la tâche n'a plus de raison d'être ; l'UPDATE final d'`integration_lieux` désactive déjà les lieux réellement absents de l'import. Reste à faire (volet 2) : purge/re-rattachement des orphelins accumulés.

- **`V117` [tech][api]** — Rotation des tokens API PostgREST (Phase 1 : régénération). **(1) Fix `auth.create_jwt`** : paramètre `expiration_date` `date` → `timestamp`. Il était déclaré `date` (V006) mais `create_role_n_token` (V036) lui passe un timestamp `…23:59:59.999999` → cast implicite en `date` → `extract(epoch)` = **minuit** → claim `exp` ≈ 24 h trop tôt. Effet concret : `get_mediateur` (`postgrest_coop`) et les tokens 2026S1 (`anct-carto-002`, `anct-dataincl-001`, `anct-incub-002`, `anct-coop-002`) ont expiré dès le **matin** du 2026-06-30 alors que `auth.token.expiration_date` affichait 23:59:59 — d'où l'erreur côté coop. **(2) Nouvelle fonction `auth.rotate_token(role, new_token_id, secret, expiration, comment)`** : régénère un JWT pour un rôle **existant**, `exp` à la seconde via `auth.sign`, enregistré `active=true` **sans révoquer l'ancien** (fenêtre de recouvrement), et **retourne** le JWT — contrairement à `create_role_n_token` qui l'émet via `RAISE NOTICE` (JWT en clair dans les logs PG). **(3) Script `scripts/rotate_api_tokens.py`** : dry-run par défaut (`--execute` pour créer, `--notify` pour Mattermost sans le JWT). Sélectionne les tokens actifs expirant l'année courante ou après, un par rôle (4 concernés : coop, carto, data-incl, incub), et imprime les JWT pour récup manuelle. Le push vers secret manager (Vaultwarden) est une Phase 2 découplée. Numéroté V117 (V113–V116 déjà pris par des MR en vol).

## 2026-06-23

- **`[tech]` [ci]** — `apply_migration_prod` passe de déclenchement **manuel** à **automatique**, conditionné (`needs: apply_migration_dev`) à la réussite de l'apply dev. Avant : job `when: manual` tournant en parallèle de dev (même stage) → déploiement prod oublié possible, et « dev OK » n'était pas un vrai préalable. Après : chaîne `test_migration` → `apply_migration_dev` → `apply_migration_prod`, prod sauté si dev échoue. Supprime la charge mentale du clic ; une migration invalide (qui casse dev) ne touche plus la prod. ⚠️ Une migration destructive **mais valide** part désormais en prod sans pause humaine.
- **`V113` [migration][tech]** — Grants complémentaires à `min_scalingo` pour le flux fusion/transfert structures-doublons (complète `V111`). `deplacerNotionsDansTransaction()` (`min` / `deplacerNotions.ts`) repointe (`UPDATE` collision-aware) les rattachements de la structure absorbée. Manquaient (n'avaient que SELECT/REFERENCES via V008) : `main.poste` → **UPDATE**, `main.contrat` → **UPDATE** ; et `main.lieu_inclusion_structure_administrative` → **DELETE** (liens résiduels SA↔lieu). **`main.poste` : UPDATE seul, pas de DELETE.** `poste` est alimentée par l'ETL id-poste → MIN ne doit pas y faire de DELETE (détruirait une ligne ETL, recréée/désynchronisée au run suivant) ; les doublons exacts non repointables restent sur la structure absorbée (soft-deletée) et sont réconciliés par l'ETL. En conséquence, le code MIN retire son `DELETE FROM main.poste` (MR `min` associée). Sans ces droits : `ERROR 42501 permission denied for table <poste|contrat|…>` en prod (l'erreur tombait d'abord sur l'`UPDATE main.poste`, pas le DELETE). Pas d'undo (cohérent avec `V053`/`V107`/`V111`).
- **`V110` [migration][tech]** — Activation de l'extension `pgvector` (type `vector` + opérateurs de distance `<->`/`<=>`/`<#>`), prérequis aux futurs cas d'usage d'embeddings (schéma `llm`, cf. `V102`/`V103`). `CREATE EXTENSION IF NOT EXISTS vector` dans `public` (cohérent avec `postgis`/`pg_trgm`). Côté infra : le Postgres local passe d'une image PostGIS standard à un build dédié (`Dockerfile.postgres`, compile pgvector v0.8.0 avec `clang15`/`llvm15` pour matcher le LLVM de l'image) ; le CI `test_migration` utilise désormais une image de service PostGIS+pgvector (`ci/Dockerfile.postgis-pgvector` via paquet PGDG `postgresql-17-pgvector`, publiée sur le Container Registry par le job `build_postgis_pgvector`). Les bases managées dev/prod supportent pgvector. `U110` = `DROP EXTENSION` (échoue si un objet `vector` en dépend encore).

## 2026-06-22

- **`V111` (ex-`V108_20260622`) [migration][tech]** — Grant `DELETE` à `min_scalingo` sur `main.personne_affectations_emploi`. La fusion de structures-doublons côté MIN (`PrismaStructureFusionRepository.fusionner()`) exécute, dans une transaction atomique, des `UPDATE` collision-aware puis des `DELETE` sur cette table (repointage des affectations de la structure absorbée vers la survivante, puis suppression des résidus irréconciliables). En `V091`, `min_scalingo` n'avait reçu que `SELECT, INSERT, UPDATE, REFERENCES` (DELETE volontairement omis). Conséquence : `ERROR 42501 permission denied for table personne_affectations_emploi` en prod au déclenchement d'une fusion. Correctif : `GRANT DELETE`. `personne_affectations_lieu` n'est lu qu'en SELECT côté MIN → pas concerné. Pas d'undo (cohérent avec les autres migrations de grants `V053`/`V107`). **Renumérotée `V108_20260622` → `V111_20260623`** : la version `108.20260622` était inférieure à `108.20260623` (`grant_coop_schema`) déjà appliquée en base → `apply_migration_dev/prod` échouait en validation *out-of-order* (`Detected resolved migration not applied: 108.20260622`). Passer en `V111` la place après le dernier appliqué → application en ordre.

## 2026-06-16

- **`V105` [api-carto][migration]** — `api.carto` : garder les lieux dont tous les médiateurs sont inactifs (revert de la règle posée en `V061`, maintenue jusqu'à `V101`). Avant : un lieu dont tous les conseillers numériques / médiateurs avaient des contrats inactifs (aucune affectation emploi non-AC active) était **exclu** de la carto publique via le dernier bloc du `WHERE` (`NOT EXISTS pal active OR EXISTS emploi non-AC actif`). Décision métier (#1348) : on réaffiche le lieu même si plus aucun médiateur n'est actif dessus. Correctif : suppression de ce bloc ; le reste de la vue est strictement identique à V101. Le filtre **par-personne** (tableau `mediateurs`) reste inchangé → un lieu sans médiateur actif paraît désormais avec `mediateurs = NULL`. Impact mesuré (base dev) : **+346 lieux** réintégrés. `U105` = rollback (restaure la définition V101). Refs #1348.
- **`6f98364` [aidants-connect]** — id-poste : fin du `UniqueViolation` sur `structure_administrative_structure_tp_id_ukey`. L'UPDATE (étape 3) matchait sur `(siret, adresse_id)` et posait le même `structure_tp_id` sur **toutes** les antennes partageant cette clé avec `tp_id NULL` → crash intra-requête. Cause amont : réseaux co-localisés (Département × Maisons), doublons de consolidation `V073`, et antennes **aidants-connect géocodées sur l'adresse du siège SIRET** au lieu de leur commune (ex. SIRET `22690001700014` Dépt Rhône : 5 lignes sur l'adresse de Lyon). Le `WHERE` cible désormais **au plus une** ligne via `s.id = (SELECT … LIMIT 1)`, scorée par similarité `pg_trgm` entre `match_nom` et `denomination_antenne` (siège à antenne NULL scoré sur `denomination_sirene`) ; garde `NOT EXISTS` conservé pour le cas inter-run. ~326 groupes ambigus (base dev) couverts d'un coup. La cause amont (géocodage AC + doublons V073) reste à réconcilier séparément.

## 2026-06-15

- **`V103` [tech][migration]** — Schéma `llm` : masquer `min.personne_enrichie` (PII oubliée en V102). Le `GRANT SELECT` large sur le schéma `min` couvre aussi les **vues** : `nao_ro` pouvait donc encore lire `min.personne_enrichie`, qui expose `prenom`, `nom`, `contact` (jsonb), `edited_by`, `deleted_by`. Correctif : nouvelle vue curée `llm.personne_enrichie` (mêmes colonnes que `llm.personne` + les flags d'enrichissement non nominatifs : `type_accompagnateur`, `labellisation_aidant_connect`, `est_actuellement_*`, `structure_employeuse_id`), suppression des 5 colonnes PII, et `REVOKE SELECT ON min.personne_enrichie FROM nao_ro`. `min.personne_enrichie` est une vue Flyway (cf. V092), colonnes stables CI/prod → liste statique. GRANT/REVOKE encadrés par test d'existence du rôle. Au passage, suppression de la vue `llm.contact_membre_gouvernance` (créée en V102) : sa seule colonne restante était `fonction`, table 100% PII et vue sans intérêt → on cesse de l'exposer (accès direct déjà révoqué en V102). `U103` = rollback (drop `llm.personne_enrichie`, recrée `llm.contact_membre_gouvernance`, restaure les accès). Refs #1591.
- **`V102` [tech][migration]** — Schéma `llm` : vues sans PII pour brancher un outil LLM en lecture seule (rôle `nao_ro`). Objectif : permettre à un LLM read-only d'explorer l'entrepôt **sans exposer de donnée nominative** (RGPD). 7 vues curées dans un nouveau schéma `llm`, suppression **totale** des colonnes PII, + révocation de l'accès direct de `nao_ro` aux tables de base. Tables traitées : `main.personne` (drop prenom/nom/`contact` jsonb/edited_by/deleted_by), `main.contact` (drop nom/prenom/email/telephone — garde fonction/flags/dates), `min.utilisateur` (drop nom/prenom/email_de_contact/sso_email/sso_id/telephone), `min.contact_membre_gouvernance` (table 100% PII → ne reste que `fonction`), `main.structure_administrative` (drop nom/prenom/courriels du jsonb `contact`, garde `site_web`+`telephone` org ; 1185/4276 lignes avec nom/prénom), `min.structure` (drop `contact` jsonb ≈ 5228/5235 = personne nommée), `min.membre` (drop `contact`/`contact_technique` = emails perso). Conservés en accès direct : `main.lieu_inclusion` (`contact` = coordonnées publiques anonymes, cf. V069). `main.structure` (en voie de disparition, cf. refonte SA/LI) : pas de vue, accès direct **révoqué** pour ne pas exposer son `contact` PII. Mécanisme : vues `security_invoker = false` → lues avec les droits du owner, donc `nao_ro` n'a aucun droit direct sur les tables de base. GRANT/REVOKE `nao_ro` encadrés par un test d'existence du rôle (no-op en CI/test où `nao_ro` n'existe pas). Rôle + mot de passe créés **hors Flyway** (`scripts/create_user_nao_readonly.sql`, secret/spécifique prod) ; à créer **avant** d'appliquer V102 en prod. `U102` = rollback (drop schéma `llm` + restauration de l'accès `nao_ro`). Refs #1591.

## 2026-06-12

- **`V101` [api-carto][coop]** — `api.carto` : une seule ligne médiateur par personne + label « Aidant Connect » conditionné à une affectation AC active (migration V101). Avant : le tableau `mediateurs` était construit par un `UNION` de 3 branches (CN seul / AC / médiateur). La branche « CN seul » ne testait que les affectations `aidants-connect` **actives** dans son garde-fou, tandis que la branche « AC » matchait dès `aidant_connect_id IS NOT NULL`. Une personne CN ayant un `aidant_connect_id` mais une affectation AC **inactive** tombait dans les deux → **deux entrées** pour la même personne (`["Conseiller Numerique"]` et `["Conseiller Numerique","Aidant Connect"]`), coordonnées identiques — donc **téléphone du conseiller exposé en double** sur la carto publique (enjeu sécurité signalé dans #1441). Cas type : lieu « Mairie de Certines » (`coop_id 63e4fbe0-…`), POUJOL et BERTIN dédoublés. Correctif : le CTE `personnes` produit désormais **une ligne par personne** (labels cumulés CN → AC → Médiateur via `ARRAY` + flags `est_cn`/`est_ac`/`a_emploi_actif_non_ac` calculés en `LATERAL`) ; le label AC requiert une affectation emploi `aidants-connect` **active**. Conséquence : une personne dont le seul motif d'apparition était une affectation AC inactive ne paraît plus. Mesuré le 2026-06-12 : **666 couples (lieu, personne) émis en double supprimés**, **28 (lieu, personne)** retirés (AC inactif seul). Inchangé : CTE `courriels`, `SELECT` final, JOINs, filtre des lieux. `U101` = référence de rollback vers V089. Refs #1441.

## 2026-06-10

- **`9c85076` [coop][api-carto]** — Affectations lieu coop basées sur `fin` (et non `suppression`) + désactivation des affectations disparues du payload. Côté coop, une activité lieu close pose `fin` mais laisse `suppression` à `null` (les emplois posent les deux) ; l'ingestion se fiant à `suppression` (`est_active = suppression is None`) laissait les activités-lieu **terminées éternellement actives** → médiateurs fantômes sur `api.carto`. Cas type : EVS La Civadière (lieu 9533) exposait Romuald PORTE (CN) alors que `en_activite[].fin = 2025-03-10`, `suppression = null`. Triple correctif : (1) `http_airflow._transform_data` porte `fin` pour `lieu_activite` ET `structure_emploi` ; (2) `insert_personne_affectations` : `est_active = fin absente OU future`, **reset global** des affectations `source='coop'` à `FALSE` avant les upserts (les absentes du payload restent `FALSE`, plus seulement les présentes-flaggées), dédup « l'entrée active gagne » sur l'historique multi-périodes ; (3) `structures_ingest` : dédup `li_rows` sur `structure_coop_id` (fix `CardinalityViolation` sur `ON CONFLICT (structure_coop_id)`, doublon coop ex. `COLLEGE DU DIOIS`). Impact local : `rapport_carto_integration.visibles_apres_filtre` **17 277 → 17 164 (-113 lieux)**, aucune régression structures / contrats / postes / financements. Refs #1574.

## 2026-06-03

- **`V099` [source][migration]** — Nouvelle couche `source` brute (raw / bronze), append-only, capturant les données **telles que reçues de chaque source avant toute transformation**. But : rejouer un run sans rappeler la source externe, tracer les erreurs (reçu vs chargé), auditer l'évolution dans le temps. Schéma `source` + 11 tables au DDL commun (`id BIGSERIAL`, `run_id TEXT`, `ingested_at TIMESTAMPTZ`, `source_key TEXT`, `donnee JSONB`) : `idposte__conum`, `coop__{structures,utilisateurs,activites}`, `ac__{structures,aidants}`, `frr__zonage`, `qpv__zonage`, `carto__structures`, `sirene__etablissements`, `ban__adresses`. GRANTS hérités via `ALTER DEFAULT PRIVILEGES` (pattern schéma `import`). Schéma + grants + les 11 tables sont posés dans une **migration unique** (un seul changement logique, livré ensemble). Capture branchée en **dead-ends parallèles** (strangler fig) dans chaque DAG : `schema-idPoste`, `coop-import`, `aidants-connect-import`, `carto-dag-import` (source_key = colonne `source` par ligne), `frr-dag-import`, `qpv-dag-import` — le flux import/main existant est **strictement inchangé**, une capture qui échoue ne bloque jamais l'aval. APIs d'enrichissement (SIRENE/BAN) capturées au plus près de l'appel via un sink optionnel injecté dans `SireneBatch` / `GeocodeurBatch` (`etl/source_capture.py::make_source_sink`, défaut `None` → CLI/tests inchangés), câblé sur `sirene-backfill`. Note : `ac__aidants` capture le **delta** du run (fetch AC incrémental). `U099` = drop du schéma CASCADE.

- **`e415153` [carto][tech]** — Source du fichier national carto basculée de l'endpoint CloudFront (`d27gljvji6o5x3.cloudfront.net/api/v0/lieux-inclusion-numerique`) vers l'URL stable data.gouv (`data.gouv.fr/api/1/datasets/r/b5e5a1e1-…`), publiée par la personne en charge de mednum-cli. **Contenu identique** vérifié : mêmes 17 555 lieux, mêmes ids, tous les autres champs identiques (le seul écart est `structure_coop_id`, que le loader recalcule lui-même et ignore donc dans le fichier). Avantages : URL pérenne et officielle (vs id de distribution CloudFront opaque), servie via data.gouv (redirection vers le dernier export nightly). Le fichier data.gouv est servi en JSON non gzippé — le `try/except gzip` du loader retombe sur le contenu brut, aucune autre adaptation nécessaire. Détail notable : data.gouv pré-remplit `structure_coop_id` y compris pour les ~1 597 ids composés (doublons dormants du ticket #1545) — piste pour résoudre #1545 en amont sans dérivation côté dataspace.
- **`1c114be` [aidants-connect]** — Géocodage des structures AC filtré par `code_postal` (postcode BAN) au lieu du `code_insee` (citycode). Le `code_insee` d'AC vient brut de l'API (`city_insee_code`) et est **pollué** : placeholder `57490`/Moyenvic, désync avec `city`/`zipcode`. En filtrant la BAN par ce code, le géocodeur soit rejette le match (insee mismatch → résultat NULL), soit retombe sur un homonyme dans la mauvaise commune → adresse canonique fausse vs SIRENE. Désormais : filtre `postcode` sur le CP source, fiable, après normalisation à 5 chiffres (AC l'envoie sans zéro initial : `1110` → `01110`, helper `_normalize_cp`). `_derive_code_insee_batch` supprimée (n'alimentait que le filtre citycode), avec `_normalize_commune` et les imports `re`/`unicodedata`/`defaultdict`. `departement` et la consolidation post-géocodage (`code_insee`, `code_postal`, `nom_commune`) rebasés sur le **résultat BAN** (`code_insee_geocode` canonique, gère la Corse 2A/2B), plus sur le `code_insee` source. **Agit sur les prochains runs uniquement** (le backfill des ~1 189 canoniques déjà fausses reste à faire). Validé sur `import.ac_structures` (1 500 lignes réelles) : sur le placeholder `57490`, taux de géocodage **0 % (filtre insee) → 60 % (filtre CP)** ; sur le sous-ensemble dépt(insee)≠dépt(CP), 62 % → 76 %. A/B iso-SIRENE de référence (clé SIRENE requise) : conformité 34→69 % sur les structures en écart, 70→92 % sur les déjà-conformes, ~2,9 % de régressions (CP multi-communes). Cf `docs/fix-ingestion-ac-geocodage-cp.md`.

## 2026-06-02

- **`V098` [coop]** — Refonte `api.get_carto_mediateur` sur le modèle `lieu_inclusion` / SA. La fonction (définition V063) lisait encore le legacy (`main.personne_affectations` type=`lieu_activite` + `main.structure`) ; fonctionnelle, mais sur des tables vouées au DROP en phase 6 de la refonte. Basculée comme `api.carto` : lieux via `personne_affectations_lieu → lieu_inclusion`, `pivot` (SIRET/RNA) via `lieu_inclusion_structure_administrative → structure_administrative`, `EXISTS structure_emploi non-AC actif` via `personne_affectations_emploi`. **Comportement strictement préservé** (mêmes filtres `is_visible` / `structure_cartographie_nationale_id NOT NULL` + `visible_pour_cartographie_nationale` / emploi non-AC actif, même recherche trigramme, même payload). Parité vérifiée en local : mêmes 7 lieux pour `magali.rapanakis@lamayenne.fr` (seul l'ordre du tableau `lieux` diffère — `jsonb_agg` sans `ORDER BY`, non-déterministe dans les deux versions), 150 requêtes sans erreur. `U098` = référence de rollback vers V063. Mise en cohérence (pas une réparation) : contrairement à `get_mediateur`, cette fonction n'était pas cassée.
- **`V097` [coop]** — Refonte `api.get_mediateur` sur le modèle SA / `lieu_inclusion` (RPC consommé par Coop). Deux bugs cumulés : **(1)** la dernière définition (V047) lisait `main.contact_structure`, renommée en `main.contact_structure_administrative` en V080 → depuis V080 **tout appel échouait** (`relation "main.contact_structure" does not exist`), fiche médiateur KO côté Coop. **(2)** la sous-requête `contrats` comparait `main.contrat.structure_id` (FK migrée vers `structure_administrative` à la refonte) à `main.structure.id` → **100 % des contrats (6 187) invisibles**, seuls les `structure_id IS NULL` legacy passaient. Refonte : `structures_employeuses` via `personne_affectations_emploi → structure_administrative` (dédupliqué par `structure_administrative_id`, une SA pouvant être pointée par plusieurs affectations source), `lieux_activite` via `personne_affectations_lieu → lieu_inclusion ⋈ lieu_inclusion_structure_administrative ⋈ structure_administrative` (SIRET + contacts de gouvernance), `contrats` joints naturellement (`contrat.structure_id = sa.id`), `contacts` via `contact_structure_administrative`. Payload structurellement identique (mêmes 7 clés top-level, `structures_employeuses[]` / `lieux_activite[]`). Validé local : 300/300 appels sans erreur, 233 exposent désormais un contrat (vs 0 avant). `U097` = référence de rollback vers V047 (non rejouable en l'état, `contact_structure` n'existant plus). Cas test : `magali.rapanakis@lamayenne.fr` → 4 employeuses (dédup SA), contrat CDD remontant sur l'antenne idposte, 9 lieux.
- **`01427cd` [carto][tech]** — Node.js installé dans l'image Airflow + `npm install` fail-fast sur le DAG carto. L'image `apache/airflow:3.1.6-python3.11` n'embarque pas Node : le `npm install --prefix …` du carto-dag-import échouait, mais le `|| echo "npm install failed"` masquait l'erreur (exit 0) → la task passait au vert alors que les dépendances JS n'étaient pas installées. Fix : bloc `USER root` dans `Dockerfile.airflow` qui installe Node.js 20.x (NodeSource) avant de repasser `USER airflow`, et suppression du `|| echo …` pour que la task échoue franchement si `npm install` plante.

## 2026-05-29

- **`67d0585` [aidants-connect]** — Ingest AC adapté à la refonte structure (phase 4a) : 3 corrections. **(1) Création SA lazy** — l'API AC liste ~8 300 structures dont ~2 050 coquilles vides (0 mandat, aucun aidant) = précisément les orphelines supprimées en phase 0.5 que la source ré-émet à chaque run. `enrich_structures_batch` ne (re)crée plus une `structure_administrative` que si `nb_mandats >= 1` OU si un aidant du run la référence → avant 2 296 structures « à insérer », après 54. **(2) UPSERT antenne-aware** — l'ancienne Pass 1 (`UPDATE … WHERE siret = v.siret`) attribuait le même `structure_ac_id` à toutes les antennes d'un SIRET (`denomination_antenne`), d'où une `UniqueViolation`. Remplacée par un UPSERT sur `(siret, denomination_antenne = nom AC)` : attache l'ac_id à l'antenne de même nom si elle existe, sinon crée une antenne AC (décision « AC = sa propre antenne »). **(3) Landing brut** — `V095` recrée `import.ac_structures` / `import.ac_aidants` en TEXT (alignées sur `_transform_data`) + 2 tasks parallèles `land_*_to_import` ; rend la donnée source requêtable en SQL (tout passait par XCom) et immunise le landing contre les valeurs hors-type. Fix annexe : `code_postal`/`code_insee` normalisés à 5 caractères avant l'INSERT `main.adresse` (`StringDataRightTruncation` sur codes mal formés / étrangers). Validé en local (run success) : 18 547 affectations emploi AC, 0 crash.
- **`V095` [migration]** — `import.ac_structures` et `import.ac_aidants` recréées en couche raw (toutes colonnes `TEXT`, alignées sur la sortie du connecteur AC). Les définitions V001 (forme brute API, plus alimentées depuis la refonte XCom) étaient des tables mortes. `U095` restaure les définitions V001.
- **`ffd7a6b` [coop]** — UPSERT `structure_administrative` antenne-aware. Le coop-import écrivait `denomination_antenne = NULL` ; avec la contrainte `UNIQUE NULLS NOT DISTINCT (siret, denomination_antenne)` (refonte), plusieurs structures coop partageant un SIRET collisionnaient sur `(siret, NULL)` → `UniqueViolation` (le `ON CONFLICT (structure_coop_id)` ne couvre pas cette contrainte). Cas emblématique : `ASS FAMILIALE FIUM ALTU` (SIRET `40841915800014`) émis avec **un structure_coop_id différent chaque jour** (anomalie source coop) → 28 lignes même SIRET/nom. Fix : `denomination_antenne` résolu contre l'état réel en base (1. nom == antenne existante → cette antenne ; 2. nom ≈ `denomination_sirene` → principale `NULL` ; 3. sinon nouvelle antenne) + dédup batch `(siret, nom)` (garde le `updated_at_coop` le plus récent) + UPSERT à 2 clés (existants → `structure_coop_id` ; nouveaux avec SIRET → `(siret, denomination_antenne)`). Validé : `ingest_structures` success try-1 (avant 4 tentatives), 0 doublon `(siret, denomination_antenne)`, FIUM ALTU 28 → 3 lignes.
- **`7ac4238` [rapport]** — `rapport_structures.py` basculé du legacy `main.structure` vers `structure_administrative` + `lieu_inclusion` + asso + `personne_affectations_emploi/_lieu`. Le rapport mesurait encore la table legacy (inchangée depuis la bascule des DAGs → aveugle aux changements réels). Nouvelle section « Modèle SA / LI » (SIRET, antennes, rattachement LI↔SA) ; sources & identifiants externes ventilés SA vs LI ; affectations via les 2 tables dédiées ; rattachement MIN joint sur `structure_administrative` (FK migrée V085/V086).

## 2026-05-28

- **`efc95fe` [carto]** — Fix `NumericValueOutOfRange: value "81600" is out of range for type smallint` sur le run carto. Le regex `^(\d+)\s*(bis|ter|quater|quinquies)?\s+(.*)$` utilisé pour extraire `numero_voie` capturait tous les chiffres en tête d'adresse sans borne, puis castait en `smallint` (max 32767). Quand un code postal était collé au début (ex. `81600 …`), l'INSERT dans `main.adresse` et le fallback du `_match` CTE explosaient. Filtre ajouté aux deux call sites (`integration_adresses` et `incoming` CTE de `load_main`) : `COALESCE((regexp_match(adresse, '^(\d+)'))[1]::int, 0) <= 32767` → les lignes `import.carto` dont l'adresse commence par un nombre hors smallint sont skippées intégralement ce run (ni INSERT adresse ni UPDATE/INSERT structure). Reviendront naturellement quand la source sera corrigée.

## 2026-05-27

- **`V094` [dataviz][migration]** — Refonte des 8 vues `dataviz.*` legacy vers `structure_administrative` + `lieu_inclusion` + `personne_affectations_emploi/_lieu`. Pattern SA+LI calque sur V087/V088/V092. Concerne : `structures` (UNION 3 cas mixte/SA pure/LI pure → 29 081 lignes vs 28 651 legacy), `poste` + `poste_pseudonymisee` (FK SA déjà migrée V078, COALESCE `denomination_antenne`/`denomination_sirene` pour `nom_structure`), `lieux_inclusion_numerique` (16 189 lignes), `personne` + `personne_pseudonymisee` (28 141 lignes, `paf_emploi` pour SA employeuse + `paf_lieu` pour zonages), `structures_employeuses` (11 638 lignes, agrégats LI ramenés sur SA via asso), `zonages` (3 893 lignes, comptage SA pour `nbr_structures` et LI pour `nbr_lieux`/personnes/accompagnements), `personnes_accompagnements` (fix sémantique : legacy comparait `adresse.id` avec `structure.id` par bug, corrigé via `li.adresse_id`). **Aucune vue dataviz/api/min ne référence plus le legacy `main.structure` / `main.personne_affectations`.** Next : sonder les dashboards Metabase en prod avant phase 6 (DROP legacy).

## 2026-04-29

- **`2474f9c` [aidants-connect]** — Banification par dérivation `code_insee`. Avant : l'API FNE ne renvoie pas toujours `city_insee_code`, BAN matche alors un homonyme arbitraire en France (~1 000 structures déportées). Trois fixes : dérivation `code_insee` depuis `(code_postal, nom_commune)` via `admin.insee_cp × admin.commune` avant l'appel BAN (validé 23/23 sur sondage) ; suffixes explicites sur le merge BAN (`suffixes=("", "_ban")`) + consolidation `nom_commune` (corrige bug latent où la colonne devenait `_x`/`_y` silencieusement, cassant le fallback INSERT `main.adresse`) ; path dégradé `ingest_structures` (INSERT adresse source brute sans geom quand BAN échoue, préserve le lien structure ↔ adresse).
- **`5fc9d93` [coop]** — Champ `beneficiaires JSONB` sur `main.activites_coop` (V066). Agrégats non nominatifs (total, genres, tranches d'âge, statuts) depuis `attributes.beneficiaires` de l'API `/api/v1/activites`. V1 = stockage non nominatif ; V2 (suivi par personne) plus tard. Ticket #1215, destiné aux graphiques d'accompagnement de la Suite Gestionnaire Numérique.

## 2026-04-27

- **`44e159f` [carto]** — Retry BAN désactivé sauf si mednum-cli n'a pas de coords. Avant : ~512 lieux déportés dans un autre département (ex: SIILAB_3183 Aisne → Deux-Sèvres). Fallback par clé naturelle (CP+commune+voie+numéro) pour `adresse_id` quand `ban_clef_interop` absent.
- **`caf8a61` [tech]** — Index ajoutés sur `admin.region` et `admin.departement`.

## 2026-04-22

- **`bb06430` [coop]** — Double `normalize_structure` supprimé. Avant : `enrich_structures_batch` convertissait les arrays PG en listes Python, sérialisées en repr `[...]` par `to_csv`, puis re-parsées en `{"['MUNI']"}` au lieu de `{MUNI}` en base. Le merge contaminait ensuite les doublons propres. Normalisation finale opère désormais sur les strings PG-array originelles.

## 2026-04-21

- **`30f77fa` [coop]** — Structures avec `id_pg` désormais importées (avant skippées car supposées prises par idposte). idposte n'importe que les structures employeuses de CN → communes-mères de médiateurs non-CN perdues silencieusement, affectations `structure_emploi` perdues. Dédup case-insensitive ajoutée.
- **`9d19646` [coop]** — `is_visible` lu depuis `attributes.mediateur.is_visible` (au lieu de `attributes.is_visible`, toujours absent). Avant : NULL pour 24 835 personnes, filtre V062 (`IS DISTINCT FROM FALSE`) inopérant → personnes ayant désactivé leur visibilité côté coop-numerique restaient exposées sur `api.carto` et `api.get_carto_mediateur`.

## 2026-04-20

- **`d8ca7ab` [personne]** — Garde temporelle par source (V064). Ajout de `updated_at_coop` et `updated_at_idposte` (symétrie avec `updated_at_ac`). Avant : V059 bumpait `updated_at` global sans modif métier (idposte écrit `edited_by` à chaque run), coop comparait `s.updated_at_coop > p.updated_at` et skippait des MAJ légitimes. `GREATEST` sur timestamps source au merge dans `personne-similarities-merge`.
- **`0a77db0` [carto]** — Lignes avec `id` > 2000 octets UTF-8 rejetées au chargement. Le dedup mednum-cli concatène les ids doublons avec `__` → valeurs dépassant la limite btree (~2704 octets) sur `structure_cartographie_ukey`, faisant échouer `integration_structures` pour le batch entier (`structure_id NULL`). Seuil 2000 + log count/max/noms.
- **`3119e67` [carto]** — Parse robuste : "21bis" → (21, "bis") pour `smallint main.adresse.numero_voie` ; troncature `code_postal`/`code_insee` à 5 caractères (gère floats `"80220.0"`).

## 2026-04-16

- **`9e56725` [api-carto]** — V063 exclut les médiateurs sans `structure_emploi` non-AC actif. ~50 personnes / ~150 lignes médiateur retirées. Cas typique : CN ayant quitté sa structure, info emploi reçue mais lieu encore actif côté Coop. Filtre par-personne « EXISTS structure_emploi non-AC actif » dans la branche CN du UNION de `api.carto` et dans le WHERE de `api.get_carto_mediateur`. Branche AC inchangée (donnée moins fiable, décision métier).
- **`6aa21af` [personne]** — V062 ajoute `is_visible` sur `main.personne`. `api.carto` (base V061) et `api.get_carto_mediateur` (base V029) excluent les personnes `is_visible = FALSE` (`IS DISTINCT FROM FALSE`). Pas de placeholder, la personne disparaît entièrement des résultats publics. Le merge `personne-similarities-dag` propage FALSE quand un des deux côtés a explicitement demandé la confidentialité.

## 2026-04-15

- **`ff389de` [rapport]** — Indicateurs avant/après filtre lieux actifs dans tous les formats (text, json, csv, markdown). Ventilation par catégorie (sans affectation, uniquement AC, médiateurs inactifs, au moins 1 actif). #1269.
- **`e89024d` [api-carto]** — Filtre les lieux dont aucun médiateur/CN n'a de `structure_emploi` active (source non-AC). Lieux sans `personne_affectation` conservés (pas d'info). #1269.

## 2026-04-14

- **`cc4d226`, `5d9cdc8` [migration]** — Renommages U057→U060 et V057→V060. Trois migrations portaient le numéro V057, empêchant Flyway de s'exécuter.

## 2026-04-13

- **`ff3d1c6` [carto]** — `integration_structures` réécrite en requêtes séquentielles. Avant : les CTE data-modifying voyaient le même snapshot, gardes `NOT EXISTS` inefficaces entre branches → `UniqueViolation` sur `structure_ukey` quand un merge transférait un `structure_cartographie_nationale_id`. Remplacé par requêtes séquentielles dans une transaction (BEGIN/COMMIT), table temp `_match` ON COMMIT DROP. Colonnes répétées (25 cols × 4 branches) factorisées en variables Python.
- **`a55c871` [carto]** — V057 (renommée V060) : FK `structure_id` (ON DELETE SET NULL) sur `import.carto`. Backfill UPDATE dans `integration_structures`. Script `rapport_carto_integration.py` (résumé + détails rejetées/sans adresse).
- **`18b4cf3` [carto]** — Correction de 1 374 structures sans adresse en cartographie nationale (1 483 invisibles avant). Trois phases : (1) CASE qui court-circuitait l'update si `siret IS NULL` (726 cas) — séparation de la garde siret NULL et de la garde `ukey_rn <> 1` ; (2) retry BAN sans `code_insee` quand mismatch INSEE bloque le match (615 cas) ; (3) déblocage 36 doublons fantômes via `structures-similarities-merge` (script `generate_carto_orphan_pairs.py` + `trigger_carto_orphan_merges.sh`) ; script `regeocode_orphelines_carto.py` pour le rattrapage rétroactif via `GeocodeurBatch`.

## 2026-04-08

- **`123de87` [tech]** — Support du format Parquet pour les exports.
